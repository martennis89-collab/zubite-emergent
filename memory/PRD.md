# Zubite.bg — Product Requirements Document

## Original Problem Statement
Build and optimize **Zubite.bg**, an educational orthodontic platform for the Bulgarian market that helps users understand their orthodontic issues and guides them toward the correct treatment path. Built with Next.js (frontend) + FastAPI (backend) + MongoDB.

**Language**: All UI/emails/content in **Bulgarian**.

## Core Product Requirements
1. **Master Quiz** — Multi-step diagnostic with Adult/Teen/Child branching, severity scoring, lead capture, segment-specific success pages.
2. **SEO & Content** — SSR `/blog`, object-storage image uploads, sitemaps.
3. **AI Outbound Calling** — ElevenLabs Conversational AI with patient context, transcript retrieval.
4. **B2B Clinic Onboarding** — `/za-kliniki` application form, structured admin review, clinic dashboards, lead distribution.
5. **Lead Verification** — Automated 24h-after-assignment email loop asking patients to confirm clinic contact.

## What's Implemented (current state)
- Full backend modularization (`server.py` → `routers/*.py` + `auth.py`, `database.py`, `emails.py`, `scoring.py`).
- High-converting animated hero (`AnimatedHomeSections.tsx`) and 10-question multi-path quiz (`MasterQuiz.tsx`).
- Segment-aware success pages (`quiz/success/page.tsx`).
- Resend-based admin notification + patient confirmation emails.
- ElevenLabs outbound calling with proper webhook signature verification (`t=ts,v0=hash` format with 30-min replay window).
- Clinic application/approval/credential flow with welcome email.
- Auto-verification background loop (every hour, scans 24h+ assigned leads).
- **Comprehensive security hardening (Feb 2026)** — 22 fixes; see CHANGELOG.md.
- **Article ZIP Importer** (`/admin/blog/import`) — structured markdown + image bundle parser with SEO validation, JSON-LD, sitemap auto-update.
- **Image placement engine** — explicit `Placeholder: {{image:x}}` tokens beat generic `Placement` rules; featured images never inserted in body. Unit-tested via `lib/_imageParser.test.ts` (Feb 2026).
- **Test Render preview** — admin can click "Test Render" before publishing to see the exact reader-view HTML, featured image, CTA, FAQ, metadata + a 13-point validation checklist (errors block publish, warnings allow save). Uses the same `parseMarkdown()` renderer as the live blog page (`lib/markdownToHtml.ts`). Regression-tested via `lib/_testRender.test.ts` (27 assertions / 10 scenarios).
- **Lead Attribution System** — first/latest-touch UTM, click IDs, referrer & internal content path captured client-side and visible in admin lead detail.
- **Consultation Workflow MVP (Feb 2026)** — clinic-side request management: when admin assigns a lead to a clinic, a `ConsultationRequest` is auto-created and surfaced on `/clinic/dashboard/requests`. Clinics perform 11 actions (call_attempted, book_consultation, mark_attended, …); each emits an immutable event in `consultation_events`. Booked consultations land in the internal `clinic_appointments` calendar. Admin sees the full event timeline at `/admin/consultation-requests/[id]`. Backend tested via 25-case regression suite (`/app/test_reports/iteration_36.json`).
- **Admin Handling for P4/P5 Patient Requests (Feb 2026)** — admin can list, filter (by `created_from`, `status`, `clinic_id`), and view full detail for the new patient-layer requests. 4 BG tabs (`Всички / Избрана клиника / Помощ от Zubite / Чака преглед`), visual badges per kind, dedicated "Тип заявка", "Съобщение от пациента", and "Съгласие на пациента" sections on the detail page. `needs_zubite_review` added as a valid consultation status. Backend tested via `test_admin_patient_request_handling.py` (12/12 PASS). **No notifications/auto-assignment/external providers triggered in this batch.**
- **Demo Premium Clinic Content Setup (May 2026)** — Sofia Premium Clinic (`aa1aa2cd-c794-4f6e-97c3-0e32475925fc`) seeded with safe, generic Bulgarian demo copy via existing `PATCH /api/admin/clinics/{id}`. Populated: `short_description`, `patient_intro`, `treatment_focus` (6 items), `doctor_spotlight_bio`, `team_note`, `clinic_story`, `environment_description`, `consultation_process`, 3 `case_library` items (all `published` + `consent_confirmed=true`). Intentionally left blank (no fake data): doctor names/roles, video URLs, nested `review_sources`. Existing `hero_image_url` and top-level Google/Superdoc review fields preserved (not overwritten). No code changes. Script: `/app/scripts/seed_sofia_premium_demo.py`. Public profile verified at `/results/{leadId}/clinics/{clinicId}` on desktop and mobile (375px → no horizontal overflow; honest placeholders for missing media/doctor). **Local-only, no push/deploy.**
- **Lead Identity Capture Standardization + Duplicate Contact UX (May 2026)** — `POST /api/leads` now strictly requires name + phone + email when `source ∈ {diagnostic_quiz, diagnostic_quiz_v1, quiz}`, with friendly Bulgarian error messages (`missing_required_contact` / `invalid_phone` / `invalid_phone_format`) and three structured 422 codes. Non-quiz sources keep the legacy blank-string-to-None coercion → no regression for soft/legacy forms. `MasterQuiz` always shows all 3 fields (Име/Телефон/Имейл) regardless of A/B `formVersion` and validates client-side before submit. New `lib/leadContact.ts` (localStorage, `zubite_lead_contact_v1`) caches contact per leadId so `RequestCallModal` (P4) and `AssistedChoiceModal` (P5) open in **confirm mode** with a read-only "Ще се свържем с вас на:" block + "Промени данните" toggle, eliminating the duplicate ask. Backwards-compatible: callers without prefill see the original empty-input layout. Tests: `test_strict_quiz_contact.py` 13/13 PASS + 215/215 regression pass; testing agent iteration_37 100%.
- **Admin Header Consistency (May 2026)** — Single shared `AdminHeader.tsx` replaces 8 different inline header variants across `/admin/*`. Two layouts: full nav for list pages (Табло / Заявки / Партньори / Кандидатури / Анализи / Блог with active `aria-current` pill) and compact `← {backLabel}` for detail/editor pages. Bulgarian per-page titles. Encapsulated logout (POST `/api/admin/logout` + localStorage cleanup). Loading / not-found states now also render the header for chrome continuity. Login page intentionally excluded. Frontend-only batch — zero backend / patient / clinic-portal / analytics / dependency changes. Tests: testing agent iteration_38 64/64 PASS (100%); zero new TS errors.
- **Clinic Treatment Fields Cleanup (May 2026)** — Established `treatments_supported` as the **canonical** clinic treatment field across backend + frontend. Non-destructive bridge: legacy `treatments_offered` is preserved on stored docs and written by the backend on every admin save as a temporary mirror. New helper `_normalize_clinic_treatments(clinic)` prefers canonical, falls back to legacy, lowercases / trims / dedupes / drops empty entries / preserves order. Public response now exposes BOTH `treatments_supported` (canonical) and `treatments` (legacy alias). Admin create + patch endpoints accept either input field and always write both. Frontend admin clinics list + create form, recommended-clinic cards, and public clinic profile all read canonical with legacy fallback. Treatment labels updated to spec (whitening → Избелване, general → Обща стоматология, cosmetic → Естетична стоматология). Tests: `test_clinic_treatment_fields_cleanup.py` 18/18 PASS + 284/284 regression. Zero new TS errors. **No DB migration, no destructive deletes — safe to proceed to Auth E5 or Resend domain verification.**
- **Review Collection + QR Foundation R1 (May 2026)** — Patient-trust signal pipeline: clinics share a public link/QR code, patients submit feedback at `/review/clinic/{clinicId}`, every submission lands as `pending` and stays HIDDEN from public until an admin approves it via `/admin/reviews`. Backend: `routers/reviews.py` with 7 endpoints (public review-info + submit, clinic collection-link + own-list, admin list/detail/approve/reject), HTML-escape sanitization, SHA-256 IP/UA hashes (no raw PII), 3-per-IP-per-600s public rate limit, deterministic duplicate fingerprint, `_safe_review_for_clinic` / `_safe_review_for_admin` projections (clinics never see admin moderation metadata or hashes). Frontend: public submission page (Bulgarian copy, 5-star rating, consent checkboxes, 404 fallback), clinic dashboard reviews page with locally-rendered QR (qrcode.react v4.2.0 — installed locally per user requirement; NO external QR APIs), Copy + Print poster, status counts; **clinic intentionally has NO approve/reject buttons — only admins moderate**. New admin moderation queue at `/admin/reviews` with 4 Bulgarian tabs (В преглед / Одобрени / Отхвърлени / Всички), clinic_id filter, per-review approve/reject with optional moderation_notes, display_permission auto-set from `consent_public_display` only on approval. AdminHeader gains a 7th nav item: **Ревюта** (Star icon, matchPrefix `/admin/reviews`). R2 (rendering approved reviews on the public clinic profile) is intentionally deferred. Tests: `test_clinic_reviews.py` 22/22 PASS; iteration_39 100% green on backend sanity + e2e UI flows; zero new TS errors in touched files.
- **Premium Branded Review Poster (May 2026)** — Upgraded the printable review poster to a Care-Pass-aligned brand asset. Component `/app/frontend/components/ReviewPoster.tsx` (preview + print variants), warm-ivory base, sky-blue accents, serif Zubite.bg wordmark, large QR in rounded white card, trust pills, friendly footer.
- **QR 404 Fix (P0, May 2026)** — QR was encoding `https://zubite.bg/...` (production not deployed) → 404. Fix: frontend derives URL from `window.location.origin`. Works in preview/dev/prod identically.
- **R2 — Public Display of Approved Reviews (May 2026)** — Approved + display-permitted patient reviews render on the public clinic profile (`/results/{leadId}/clinics/{clinicId}`) between Trust Signals and FAQ. New backend `GET /api/public/clinics/{clinic_id}/reviews` filters strictly by `clinic_id + status="approved" + display_permission=true`; returns only public-safe fields (`id`, `rating_overall`, `feedback_text`, `patient_display_name`, `treatment_type`, `approved_at`, `submitted_at`). NEVER exposes phone, email, private notes, moderation notes, IP/UA hashes, duplicate fingerprint, status, clinic_id, or full last name. `_safe_first_name()` strips `patient_name_optional` to first word only (≤24 chars). Average + count computed over approved+permitted only — no fake stars on empty. New `<PublicReviewsSection>` (responsive 2-col → 1-col mobile, no 375px overflow), per-card "Прегледано от Zubite" badge, 380-char "Покажи още" toggle, calm empty state, fail-soft fetch. Optional secondary "Оставете мнение за тази клиника" CTA opens `/review/clinic/{id}` via `window.open` (smaller than primary "Искам обаждане"). feedback_text rendered as plain React text — no `dangerouslySetInnerHTML`. Tests 22→34 PASS.
- **Clinic Aligner Brand Tags / Provider Badges (May 2026)** — Structured `aligner_brands_supported[]` field with curated whitelist (Invisalign, Spark, Angel Aligner, Dentalign, ClearCorrect, Other), per-entry `relationship` + `verification_status` + `visible` + sanitised `other_label`. Shared module `aligner_brands.py` with `normalize_aligner_brand_entries()` and `public_aligner_brand_chips()`. **Verification guardrail**: public response can only render "Официален <brand> provider" when admin has set `verification_status=verified`; otherwise auto-downgrade to safer "Работи с …" form. Admin editor UI with brand select / relationship / verification / visible toggle / "other" custom label / amber inline warning when official-without-verified. Public chips on recommended cards (compact) and lead-contextual profile (labelled section, emerald for verified-official with shield icon, sky for offered). Text-only chips — no brand logos. Tests: `test_aligner_brand_tags.py` 25/25 PASS.
- **Auth Batch E5 — Session Governance (May 2026)** — Upgraded admin/clinic auth from token-only to server-side session governance backed by a new `auth_sessions` Mongo collection. **JWT payload** now includes a `jti` (UUID) on every issued token; `iat` + `role` are also explicitly set. **Login** (`POST /api/admin/login`, `POST /api/clinic/login`) creates a matching `auth_sessions` row with `{user_id, user_type, jti, created_at, last_seen_at, expires_at, revoked_at, revoked_reason}` — no raw IP/UA stored. **Protected reads** (`get_current_user`, `get_current_clinic`) decode the JWT, then look up the session by `jti` and reject when missing, revoked, expired, wrong `user_type`, or mismatching `user_id` (returns 401 / 403 consistently). `last_seen_at` refreshed at most once per 60s. **Logout** (`POST /api/admin/logout`, `POST /api/clinic/logout`) revokes the current jti in addition to clearing the cookie — best-effort jti extraction so logout never errors on stale tokens. **New endpoints**: `POST /api/admin/logout-all` and `POST /api/clinic/logout-all` revoke every active session for the current user; `POST /api/admin/auth-sessions/cleanup-expired` (admin-only) flips expired sessions to revoked for housekeeping. Admin-token cannot reach clinic routes and vice-versa (cross-role rejection at session level, not just JWT-role). **Backwards compatibility note**: legacy pre-E5 tokens (no jti) are now rejected — users must re-login once. Public routes (review submission, recommended-clinics, public clinic profile, blog) remain fully public. Tests: `test_auth_session_governance.py` 16/16 PASS (login creates session+jti for admin and clinic, valid session works, missing/unknown/revoked/expired jti rejected, cross-role rejected, logout revokes current session, logout-all isolates per-user, cleanup-expired marks past sessions, public routes don't require session). End-to-end frontend verification: admin login → dashboard → logout → re-visit /admin/dashboard redirects to login. Zero new TS errors. No quiz/matching/request/reviews/provider-badge/email scope drift.
- **Premium Wave.co-inspired Homepage Redesign (Feb 2026)** — Public landing (`/`) rebuilt as a calm, premium decision-engine experience per `/app/design_guidelines.json` (Organic & Earthy / Soft-Tech Healthcare archetype). 14 sections in one composed `HomeContent.tsx`: Glass nav with scroll-aware backdrop blur, Hero with floating product mockup + match-percentage cards + dual CTAs, Trust strip, Problem section, How-it-works (4 steps), 7 Treatment category cards, Decision-preview mockup, Zubi abstract orb section (no mascot/face), Clinic matching explanation, Patient questions grid, **NEW** SSR Recent Articles (3 cards from `/api/blog/posts?limit=3`), Care Pass dark teaser card, FAQ accordion (5 items), Final CTA, **NEW** premium dark `HomeFooter`, and Mobile-only sticky CTA. Warm-ivory base (`#FCFAF8`), soft turquoise accents (`teal-600`), Playfair Display headlines (now globally mapped via `tailwind.config.js fontFamily.serif`), Inter body. Motion: single `IntersectionObserver`-based `Reveal` helper drives staggered entrance fades (no framer-motion dep); CSS `float` keyframe levitates hero mockup. All interactive elements carry `data-testid` (home-nav, home-hero, home-hero-mockup, hero-primary-cta, hero-secondary-cta, trust-chip-0..4, treatment-card-0..6, decision-cta, recent-article-0..2, recent-articles-view-all, care-pass-cta, faq-item-0..4, final-primary-cta, home-mobile-sticky-cta, home-footer). `page.tsx` rewritten as a thin server component preserving SEO Metadata, OG/Twitter cards, canonical URL, and both WebSite + Organization JSON-LD scripts. Legacy `AnimatedHomeSections.tsx` retained per user choice. Tests: `testing_agent_v3_fork` iteration_40 → 100% pass, zero bugs, all 14 sections verified across 1920/768/375 viewports, all CTA routes correct, FAQ toggle works, no horizontal overflow.
- **Homepage Copy Refinement (Feb 2026)** — Pure copy/messaging pass: rewrote every public string on `/` to be clearer, more concrete, more patient-friendly, and medically safer. Removed diagnostic-feeling phrasing (percentages, "съгласувано насочване", "60-секунден преглед", awkward "не игра", "скрипт на долна челюст"). Reframed the platform consistently as **ориентация & насочване**, never diagnosis. The **Zubite Care Pass guarantee** is now stated unambiguously and woven through 12 touchpoints (trust strip, How-it-works step 04, dedicated section + secondary CTA "Как работи Care Pass" + disclaimer, two new FAQ entries) — *"След като посетиш консултация в партньорска клиника чрез Zubite.bg, клиниката ще ти предостави Zubite Care Pass с отстъпки за продукти за орална хигиена."* Removed all forbidden Care Pass framings: family membership, subscription, early access, personal calendar, treatment tracking, predictable clinic prices, treatment discount. Hero mockup percentage bars replaced with a calm "Посоки за обсъждане" text list; decision-preview labels (`Срок` → `Ориентировъчен срок`, `Подходящи подходи` → `Възможни подходи за обсъждане`) and verification badge (`Прегледано` → `Примерен ориентир, не диагноза`) tightened. Treatment-card CTAs `Научи повече` → `Виж насоки`. Patient-question cards gained 6 short description blurbs. FAQ went from 5 → 7 entries. Page metadata description / OG / Twitter aligned with the new hero subheading. Verified copy hygiene: 0 hits for the forbidden-phrase list (`съгласувано`, `не игра`, `точно с твоя`, `най-добрата клиника`, `за цялото семейство`, `early access`, `персонален календар`, `проследяване на лечение`, `предвидими цени`, `60-секунден преглед`). Self-tested via 7 screenshots across Hero/Problem/How/Treatments/Decision/Zubi/CarePass/FAQ on 1440×900 — zero layout regression. No backend changes, no new files, no deleted files, no new deps, no impact on quiz / blog / admin / clinic dashboard / reviews / attribution / lead capture.
- **Wave.co Glassmorphism Visual Polish (Feb 2026)** — Heavy visual upgrade of `/` to match Wave.co-tier premium product-landing aesthetic. Pure visual/UX work — zero copy rewrites, zero route changes, zero backend changes, zero new packages. **Floating pill nav** (`bg-white/40 → /70 backdrop-blur-2xl ring-1 ring-white/40-60` + scroll-aware shadow). **Hero**: premium radial-gradient backdrop (teal at 15/20, cyan at 85/60) + 3 soft blur blobs; glossy gradient CTA with inset white-25% blur "shine" line; frosted glass secondary CTA; new row of 4 hero glass chips (`Без регистрация / Ориентир за цена и срок / Care Pass след консултация / Не заменя преглед`); hero mockup gains 2 extra stacked depth cards (rotated 3.5° and counter-rotated −2.5°) for Wave-style layered product depth. **Trust strip** converted to a glass pill rail (`rounded-full bg-white/60 backdrop-blur-xl`) with mobile horizontal scroll. **Problem** cards gain Lucide icons (Megaphone / MessagesSquare / Wallet / HelpCircle) + radial-gradient backdrop. **How It Works**: connected glass card track with horizontal teal-200 gradient connector line behind the row; each step now carries a **mini UI mock** (questionnaire rows / orientation card / comparison rows / Care Pass voucher); **Step 04 Care Pass** rendered with distinctive teal-tinted gradient bg + teal ring + larger teal numeral. **Treatment categories**: glass cards with subtle radial hover-sweep; first two cards (Ортодонтия + Алайнери vs брекети) featured with teal-tinted bg + teal ring + larger drop shadow. **Decision preview**: glassy main product card with third stacked depth card; **two new floating glass chips** orbit the mockup (Въпроси за преглед / Продължаваш само ако решиш) — closest section to a Wave-style product demo. **Zubi**: frosted glow halo + glass container around the orb + **NEW chat bubble** ("Zubi · Искаш ли да разбереш какви въпроси да зададеш на ортодонт?") with sparkles avatar dot + 8s float animation. **Clinic matching**: completely re-laid out 2-col with **NEW app-like matching mockup** on the right showing Критерии за насочване (Град · София / Категория · Ортодонтия / Предпочитание · дискретно лечение) + Релевантни клиники (3 anonymized cards Клиника А / Б / В with treatment tags Алайнери, Възрастни, Ортодонтия, Естетични брекети, Дискретно) + explicit "Примерни клиники, не реални имена" disclaimer; no paid-ranking implication, no "best clinic" copy. **Patient questions**: translucent cards with rounded icon circles + hover teal-ring + group-hover gap animation. **Recent Articles**: glass-hybrid cards with persistent `BookOpen` fallback under the image; `onError` hides only the broken `<img>` so the gradient placeholder stays visible — fixes broken-image rendering. **Care Pass section — full premium centerpiece rewrite**: deep navy-teal gradient panel (`linear-gradient(135deg, #0E1A24 → #112832)`) with radial teal glow at top-right and cyan glow at bottom-left; **NEW glossy Care Pass card mockup** on the right with stacked depth card behind, top white-20% blur "shine" line, "Zubite / Care Pass" serif wordmark, gift-icon glass tile, "Включва: Отстъпки за продукти за орална хигиена", "Получаваш го от клиниката след консултация", thin gradient shine line at bottom edge; **4 benefit chips** ("След проведена консултация / От клиниката / Орална хигиена / Не е отстъпка от лечение") as white-6% glass chips with teal-300 checks. **FAQ** wrapped in a glass container with soft slate-200/60 dividers. **Final CTA**: frosted gradient panel (`bg-white/55 backdrop-blur-2xl`) with 3 floating background glass chips (~60 секунди / Без регистрация / Care Pass) + same glossy gradient primary CTA. **Mobile sticky CTA**: glossy gradient + inset shine line. **Footer**: thin teal-400/40 gradient top glass edge + soft teal blur halo. **Motion**: reuses existing IntersectionObserver `Reveal` helper; CSS `float` keyframe powers Hero mockup, Hero floating chips, Decision floating chips, Zubi chat bubble (6–8s ease-in-out); all hovers do `-translate-y-0.5/-1` lift + soft shadow growth + group arrow `translate-x-0.5`. **Mobile (390×844)** verified: zero horizontal overflow, glass cards readable, hero mockup doesn't overpower the headline. Imports added: 6 new Lucide icons (MapPin, SlidersHorizontal, Megaphone, MessagesSquare, Wallet, HelpCircle, Gift) — zero npm/yarn changes. `npx tsc --noEmit` → 0 new errors. File grew from ~960 → ~1310 LOC, all in one component per prior architectural decision.
- **Clinic List P2 Text-Density Polish (Feb 2026)** — Reduced visible text density on `/results/{leadId}/clinics` per the `Default = clarity, Expanded = education` rule. **Page hero** — H1 changed from `Ето до 3 подходящи клиники според вашия резултат` to the broader awareness-framed `Подходящи следващи стъпки според отговорите ти`; subheadline tightened to single sentence `Виж клиники, които може да са релевантни според описания случай, града и избраната категория.`; added a new `match-title-chips` row of 4 trust chips (`Ориентир, не диагноза` ShieldCheck · `Насочване според случая` Compass · `Care Pass след консултация` Sparkles · `Без задължение` CheckCircle2) so the safety/quality framing is scannable instead of paragraphed. **Selection-rule banner** — compressed from 3-clause sentence with comma-rule to a single 1-line `Може да разгледате X клиники · Заявка към Y.` with tabular-nums and `·` separator; removed the `Ако се колебаете, използвайте „Помогнете ми да избера“` tail (the assisted-choice CTA below the cards is self-explanatory). **Assisted-choice panel** — heading from `Не сте сигурни коя клиника да изберете?` to `Колебаете ли се?` (3 words); description tightened to `Помагаме да изберете подходяща следваща стъпка.`; container padding reduced from `p-6 sm:p-8` to `p-5 sm:p-6`. **Trust note** — collapsed from 3-sentence paragraph to a single-line footnote `Zubite.bg не поставя диагноза и не гарантира лечение. Окончателната оценка се прави от стоматолог или ортодонт.` at `text-[11px]` size — medical-safety language preserved per spec; old 'Препоръките са базирани на наличните партньорски клиники и контекста на вашата заявка' moved into the title chips ('Насочване според случая'). **ClinicRecommendationCard** — added a new always-visible `clinic-card-carepass-chip-{id}` (Gift icon + `Care Pass след консултация` label + hover title attribute containing the full Care Pass explanation `След проведена консултация чрез Zubite.bg, клиниката ти предоставя Care Pass с отстъпки за продукти за орална хигиена.`); reason paragraph (`clinic-card-reason-{id}`) clamped to 2 lines via `line-clamp-2` for scan-friendly default view; merged response_expectation / partner_since_year / placement_disclosure 3-line meta block into a single `<details>` `clinic-card-details-{id}` accordion (`Виж детайли` ↔ `Скрий детайли` with rotating chevron) so the card's default height drops ~30% while full information remains 1 click away. **Tests**: `testing_agent_v3_fork` iteration_46 → 94% pass with 1 HIGH (Care Pass chip rendered only when treatments array non-empty, missing on Featured + Standard with empty treatments) → fixed in same batch by decoupling the chip from the `treatmentBadges.length > 0` conditional → re-verified via main agent self-test (`Care Pass chip count: 3` across all 3 tiers). Mobile 0px overflow at 390 + 375 verified. Native `<details>` Enter-key activation verified accessible. `clinic-card-view-profile-{id}` and `clinic-card-cta-{id}` deep links and RequestCallModal opening preserved. All data-testids preserved. **Untouched** per user direction: homepage, quiz, quiz success, clinic profile, blog, static/legal, admin, clinic dashboard, backend, auth, attribution, SEO, JSON-LD, routes, data models. Zero TypeScript regressions in touched files (pre-existing unrelated TS warning in `lib/attribution.ts` not addressed in this batch).
- **Clinic Profile P0 Text-Density Polish — Tabs Restructure (Feb 2026)** — Restructured the densest patient-facing page (`/results/{leadId}/clinics/{clinicId}` — was 1830 LOC single scroll, ~40 long-text containers) into a **5-tab progressive-disclosure layout** while preserving all data-testids, routes, lead capture, quiz logic, attribution, SEO/JSON-LD, and the Wave.co glassmorphism visual system. **New `ProfileTabs` component** (semantic `role=tablist` + `role=tab` + `aria-selected` + `aria-controls` + tabIndex roving for keyboard a11y; sticky tab bar at `top-[68px]` with `overflow-x-auto` for mobile horizontal scroll) with 5 tabs: **Обобщение** (Building2 icon — `profile-reason-section` with collapsible deeper-reason details + `ClinicAboutBlock` collapsed first-sentence summary + 'Прочети повече' details for full intro/story + Premium extras via `PremiumOverviewExtras`), **Услуги** (Stethoscope icon — `profile-treatments-section` chips + `profile-aligner-brands-section` with collapsible 'Какво означават етикетите?' details + `TrustSignalsSection` + `PublicReviewsSection`), **Как работи консултацията** (Footprints icon — `PostRequestTimeline` always + clinic's `consultation_process` first-line teaser with 'Прочети целия процес' details + `PatientJourneySection` fallback), **Care Pass** (Sparkle icon — full-variant `CarePassPanel` with glossy card mockup + new `profile-care-pass-how` section with short explanation + 'Какво НЕ е Care Pass?' details revealing 4 negative bullets [не е отстъпка от лечение / застрахователен продукт / абонамент / безплатно лечение]), **Въпроси** (MessagesSquare icon — existing `ClinicFAQSection`). New `ClinicAboutBlock` shows only the first sentence (≤140 chars) of `patient_intro` by default and reveals full intro + clinic_story via 'Прочети повече' `<details>`. New `PremiumOverviewExtras` wraps case_library / doctor / environment with collapsed `<details>` per item. `Bottom CTA` simplified — removed the 'Заявка за обаждане може да изпратите само към една клиника. Ако се колебаете, разгледайте...' paragraph and replaced with a 1-line footnote `Заявка може да изпратите само към една клиника.`; heading bumped to `text-xl sm:text-2xl`. `Trust note` compressed to single-line `Zubite не поставя диагноза и не заменя преглед при лекар.` (text-[11px] footnote). Care Pass copy verified: `CarePassPanel` `full` variant headline now reads `След консултацията получаваш карта с отстъпки за продукти за орална хигиена.` (added missing word `продукти` per testing agent feedback); compact variant strapline also includes `продукти`. **All existing section components reused as-is** (TrustSignalsSection, PostRequestTimeline, ClinicFAQSection, CarePassPanel, ClinicFitPanel, PublicReviewsSection, AlignerBrandChips, PremiumHero, CompactHero, CaseLibrarySection, DoctorSpotlightSection, PatientJourneySection, ZubiteFeedbackPlaceholderSection, PlaceholderSection, ClinicDecisionSidebar, MobileDecisionStrip, RequestCallCta) — only the container/composition changed. Decision sidebar (lg+) and mobile decision strip (<lg) unchanged. `ClinicFitPanel` rendered always above the tabs. **Untouched** per user direction: homepage, quiz, quiz success, clinic list, blog, static/legal, admin, clinic dashboard, backend, auth, lead capture, attribution, SEO, JSON-LD, routes, data models. **Tests**: `testing_agent_v3_fork` iteration_45 → 97% pass (Premium + Standard + Featured tiers all verified; keyboard Enter/Space activates tab; tabIndex roving correct; URL preserved on tab switch; mobile 0px overflow at 390 + 375; sticky tablist with 713px overflow-x scroll; RequestCallModal still opens). 1 LOW (CarePassPanel missing word `продукти` in headline) → fixed in same batch in both `full` + `compact` variants → re-verified via main agent self-test (`profile-care-pass-section` innerText now contains `продукти за орална хигиена`). 1 INFO (Featured tier without patient_intro had no detectable placeholder testid) → fixed by changing `testid` from `profile-featured-extra-section` to the agent-expected `profile-featured-extra-placeholder`. TypeScript clean. Zero backend / route / dependency changes. ~1830 → still 1958 LOC (added tab container code but **visible** text-density dropped massively because most sections are now lazy-rendered behind tab switching).
- **Site-wide Text-Density / Readability Polish — Patient Funnel Batch (Feb 2026)** — Targeted reduction of visible text density across the patient funnel (Homepage → Quiz → Quiz Success) per a `Default view = clarity, Expanded view = education` 3-layer strategy. **Homepage**: removed the secondary disclaimer paragraph below the hero (the safety chip `Ориентир, не диагноза` + global footer disclaimer cover medical-safety obligation); rebuilt `HowItWorks` 5-step cards by deleting the 5 nested 'mini' preview boxes (sample symptom list / sample ориентир / sample clinic list / sample preview / Care Pass mini) — cards are now icon + 01-05 numeral + short title + one-sentence sub + Care Pass chip on step 5; replaced the `DecisionPreview` intro paragraph with a single bold sentence `Кратко, разбираемо обобщение — без жаргон, без диагноза.`; converted all 8 `TreatmentCategories` cards from always-visible `<Link>` blocks to collapsible native `<details>` (collapsed shows ONLY icon + title + `Виж насоки` chevron; expanded reveals the description sentence + `Прочети повече` link to category page). **Quiz**: added a global `quiz-safety-chip` (`Ориентир, не диагноза` ShieldCheck pill) in the fixed quiz `Header` visible at `sm+` (≥640px) — gives a persistent medical-safety signal without repeating disclaimers on every question; shortened soft-commit subheadline from a two-sentence variant to `Препоръчани клиники според резултата и твоя/вашия град.`; shortened exit-screen paragraph from `Разбираемо. Ако решиш по-късно...` to a single sentence `Можеш/Можете да провериш/проверите отново по всяко време.`. **Quiz Success**: rewrote all 9 `SEGMENT_SUMMARIES` (3 bands × 3 segments) from multi-sentence paragraphs to single scannable sentences (e.g. moderate/adult → `Умерен риск — има признаци, които заслужават внимание от специалист.`); shortened the 3 `NEXT_STEPS` items to one short line each (`Виж до 3 подходящи клиники.` / `Отвори профил и прецени коя е подходяща.` / `Избери за обаждане или поискай помощ от Zubite.`); replaced the duplicated Care Pass explanatory paragraph in `success-care-pass-note` with a compact pattern (premium dark navy-teal panel + Care Pass card mockup + single short headline `След консултацията клиниката ти дава Care Pass.` + 4 chips `Отстъпки за орална хигиена / От клиниката / След консултация / Не е отстъпка от лечение`); compressed trust footer to footnote style (`text-[11px]` data + `text-[10.5px]` disclaimer) while KEEPING the `Zubite не поставя диагноза` line visible — disclaimer is not hidden, just visually de-emphasised; added a `success-safety-chip` (ShieldCheck pill `Ориентир, не диагноза`) inside the result band card so medical safety language appears in all three patient-funnel surfaces (hero / quiz header / success card). **Untouched** per user direction: clinic profile, clinic list, blog, static/legal pages, admin, clinic dashboard, results pages. **Tests**: `testing_agent_v3_fork` iteration_44 (initial) → 97% pass with 1 LOW-priority gap (missing success card safety chip) — fixed in same batch with import + 2-line chip insertion → re-verified via main-agent screenshot (`success-safety-chip` innerText = `ОРИЕНТИР, НЕ ДИАГНОЗА`). All routes / quiz logic / scoring / lead capture / attribution / SEO / JSON-LD / data-testids preserved. Zero new TypeScript errors. 0px horizontal overflow at 1440/1280/768/390/375 px on `/`, `/quiz`, `/quiz/success`.
- **Homepage Narrative Restructure — Patient Awareness First (Feb 2026)** — Reordered the entire homepage narrative per the new 10-section persuasion hierarchy (Hero/Awareness → Patient benefit → What you may be noticing → How it works → Result preview → Why trust Zubite → Treatment coverage → Care Pass → FAQ → Final CTA), preserving the existing Wave.co premium glassmorphism visual system. **Hero rebuilt as patient-awareness question** — headline now `Възможно ли е да имаш дентален проблем, без да го осъзнаваш?` (replacing the abstract `Първо яснота. После избор.` framing, now used only as eyebrow chip), 3 transparency chips (`Без регистрация / Ориентир, не диагноза / Care Pass след консултация`), larger H1 (`text-[2.5rem] sm:text-5xl lg:text-[3.75rem]`) and lead (`text-lg sm:text-xl`). **New `PatientBenefit` section** with 4 native `<details>` benefit cards (По-малко объркване / По-добри въпроси / По-малко натиск / Допълнителна стойност) — short visible copy, long explanation in hover/tap reveal. **New `SymptomChips` section** (anchor `#noticing`) with 10 compact glass chips covering common dental concerns (Кървящи венци / Криви или струпани зъби / Щракане в челюстта / Болка или напрежение / Лош дъх / Липсващ зъб / Износване на зъбите / Неясна захапка / Детето диша през устата / Чудиш се за брекети или алайнери) + `Започни краткия ориентир` CTA. **HowItWorks rebuilt to 5 steps** (Отговаряш / Получаваш ориентир / Избираш следваща стъпка / Посещаваш консултация / Получаваш Care Pass) with `lg:grid-cols-5`, step 05 visually emphasised via teal-tinted gradient + ring. **DecisionPreview headline** changed to `Какъв ориентир получаваш?` with 4-item ориентир list (Ориентир, не диагноза / Възможна следваща стъпка / Какъв специалист може да има смисъл / Въпроси за преглед). **New `TrustReason` section** (replaces old `Problem` ad-fatigue framing) — 3 `<details>` trust cards with positive framing (Не поставяме диагноза / Не показваме случаен списък / Не те притискаме). **TreatmentCategories extended to 8 categories** (added Венци и хигиена) with new headline `С какви случаи може да ти помогне Zubite.bg да се ориентираш?`. **CarePassTeaser headline** changed to `Посети консултацията. Получи Care Pass.` and adds a 3-step glass visual flow (Заявяваш насочване → Посещаваш консултация → Клиниката ти дава Care Pass) before the 4 benefit chips. **FinalCTA headline** changed to `Не отлагай само защото не знаеш от къде да започнеш.`. **Removed sections** (deleted entirely from JSX and function bodies): `Problem`, `ZubiSection`, `MatchingExplain`, `PatientQuestions`. Nav links updated: replaced `#zubi` with `#noticing` and added `#care-pass` link. Unused imports (`MessageSquare`, `Megaphone`, `SlidersHorizontal`, `Wallet`, `MapPin`) and unused asset constants (`ZUBI_ORB`, `ZUBI_MASCOT`) cleaned up. Typography hierarchy tightened — every section headline now `text-[2rem] sm:text-4xl lg:text-5xl` for consistent scan-ability, benefit/trust card titles `text-xl sm:text-2xl`. Medical-safety + Care Pass copy rules strictly preserved throughout (`ориентир` 22 occurrences, `не диагноза` 4 occurrences, `продукти за орална хигиена` 6 occurrences). **Tests**: `testing_agent_v3_fork` iteration_43 → **100% frontend pass**, zero bugs, all section ordering / data-testids / copy strings / CTAs / anchors / 5-column grid / viewport-responsiveness verified at 1440/1280/1024/768/390/375 px. Zero backend / route / quiz-logic / lead-capture / attribution / admin / SEO changes. Zero new TS errors in touched file. (Known issue noted by testing agent: unrelated single 500 console response on initial load — does not affect homepage rendering, will be diagnosed separately when triaging the network panel.)
- **Patient-facing Clinic Surfaces Wave.co Redesign (Feb 2026)** — Recommended clinics list (`/results/{leadId}/clinics`), Lead-Contextual clinic profile (`/results/{leadId}/clinics/{clinicId}`, ~1825 LOC) and the `ClinicRecommendationCard` migrated to the same warm-ivory/teal/glassmorphism system as the homepage. **New shared `CarePassPanel` component** (`/app/frontend/components/patient/CarePassPanel.tsx`, 3 variants — `full` / `compact` / `inline`) standardises every Care Pass mention with the dark navy-teal gradient + glossy Care Pass card mockup matching the homepage. Replaces 2 legacy flat sky-50 `/care-pass.png` strips. **Recommended list page**: warm-ivory bg (`#FCFAF8`) + parallax teal/cyan blob radials, glass selection-rule + status banners (`bg-white/65 backdrop-blur-xl ring-1 ring-white/70`), CarePassPanel compact strip with `Как работи Care Pass` learn-more link, glass assisted-choice panel, teal gradient CTA buttons (`linear-gradient(135deg,#14b8a6→#0d9488→#0f766e)` with inset white-25% shine + 18px shadow). **ClinicRecommendationCard**: glass card with inner top gloss, teal-50 icon container, teal treatment chips, teal gradient `Виж профила` primary CTA, glass `Искам обаждане` secondary, hover `translate-y` lift + teal-200 ring + teal-tinted shadow. **Profile page**: ivory + parallax bg, all 18 white-bordered `<section>` cards migrated to glass treatment via single search-replace, sticky decision sidebar gains dark navy-teal Care Pass footer strip (replaces the old sky-50/40 footer), hero image figure swapped to ring-1 ring-white/70 + glass shadow, premium image placeholder gradient now teal-100/60→cyan-50/40→white with teal-100 ring, fit-panel + post-request-timeline + trust-signals all teal-tinted glass, mobile decision strip becomes a dark navy-teal block. CarePassPanel (compact, with showLearnMore) replaces the inline Care Pass section in the profile body. All bg-sky-* / text-sky-* / ring-sky-* / border-sky-* migrated to teal (verified: `grep sky-` returns 0). Public reviews summary badge + leave-CTA also migrated from sky → teal. Care Pass copy across all variants strictly enforces "след посетена консултация" + "орална хигиена" framing; never frames as treatment discount / subscription / insurance / family plan / free treatment. Sidebar copy strengthened to mention "отстъпки за продукти за орална хигиена". **Tests**: testing_agent_v3_fork iteration_42 → 92% frontend pass, 3 minor follow-ups all fixed in same batch (PublicReviewsSection sky→teal x3, sidebar oral-hygiene framing, CarePassPanel FullVariant Image priority). Post-fix self-check: profile DOM `sky-` count = **0**. 0px horizontal overflow at 1440/768/390/375px on both pages. All `data-testid`s preserved; no API/backend/data-model changes; no dependency changes. Zero new TS errors in touched files (pre-existing TS warnings elsewhere unrelated).
- **Dedicated `/care-pass` Landing Page (Feb 2026)** — New public route `/care-pass` (`frontend/app/care-pass/page.tsx`, ~340 LOC, self-contained server component) destination for the homepage "Как работи Care Pass" secondary CTA. Built in the Wave.co-inspired premium glassmorphism system: warm-ivory base (`#FCFAF8`), teal radial gradients, soft blur blobs, Playfair Display headlines, layered glass cards. 7 sections — Hero (with Care Pass card visual asset E + 3 transparency chips "Без задължение / След проведена консултация / Не е отстъпка от лечение" + glossy gradient primary CTA to `/quiz` + frosted secondary CTA anchoring to `#how`), How-it-works (4-step grid with step 04 "Получаваш Care Pass" visually featured via teal-tinted gradient + ring + larger numeral), What's-Inside benefits (4 oral-hygiene product groups: четки/пасти/конец+междузъбни/вода-за-уста+допълнителни), **"Care Pass не е"** amber-bordered guardrail block listing 6 explicit negations (отстъпка от лечение / застрахователен продукт / абонамент / безплатно лечение / гаранция за резултат / замяна на медицинска препоръка), 6-question FAQ (native `<details>` for JS-free a11y + SEO), Final CTA panel, JSON-LD `FAQPage` schema emitted from same constant as the visible FAQ (zero drift). Strict Care Pass copy compliance: required phrases "отстъпки за продукти за орална хигиена" + "след посетена консултация" present in positive framing; forbidden phrases (абонамент / застраховка / безплатно лечение / семейство / предвидими цени / персонален календар / проследяване на лечение) appear ONLY inside the "не е" negation block + FAQ negatives. Reuses global `Header` + `Footer`. All interactive elements carry `data-testid` (care-pass-page, care-pass-hero/how/benefits/not/faq/final-cta, care-pass-step-0..3, care-pass-benefit-0..3, care-pass-faq-0..5, hero-primary-cta, final-primary-cta). Metadata: BG title/description, OG, Twitter, canonical `https://zubite.bg/care-pass`. Tests: `testing_agent_v3_fork` iteration_41 → 100% frontend pass, zero bugs, verified at 1440/768/390/375 px (0px horizontal overflow), all CTAs/FAQ accordion/JSON-LD/header+footer/copy compliance. Zero backend changes, zero dependency changes.
- **Background Motion & Parallax (Feb 2026)** — Added subtle, premium-grade background motion across `/` per Wave.co depth language. **Single global rAF scroll listener** (`useBackgroundParallax` hook) writes `document.documentElement.style.setProperty('--py', scrollY)`; per-element parallax done via `style={{ transform: 'translate3d(0, calc(var(--py,0) * Xpx), 0)', willChange: 'transform' }}` — zero React re-renders. **`px(factor)` helper** centralises the inline style. **Centralised `MotionStyles` component** injects 4 new keyframes — `floatSlow` (8-9s), `floatSlower` (10-14s) for stacked depth cards & Care Pass; `driftSlow` (12-16s, X+Y+0.6° rotate) for 3 NEW decorative floating shapes behind the Hero mockup; `breatheGlow` (8-15s, opacity+scale) applied to every background blur blob across Hero/Problem/Decision/Zubi/Matching/CarePass/FinalCTA at varied tempos; `shimmerSweep` (8s, diagonal white-18% gradient translating −120% → 220%) overlays the Care Pass glossy card for a tasteful highlight pass. **13 background layers** marked `data-parallax` with factors ±0.04 → 0.08; **28 elements** carrying running keyframe animations. **Accessibility**: `useBackgroundParallax` short-circuits when `prefers-reduced-motion: reduce` matches → `--py` never set, freezing every parallax layer; `MotionStyles` injects a `@media (prefers-reduced-motion: reduce)` block forcing `animation: none !important` on every animated element + `transform: none !important` on every `[data-parallax]` element. Verified via Playwright `emulate_media(reduced_motion="reduce")` → `--py` empty, `activeAnims: 0`, parallax transform `none`. **Performance**: only `transform` + `opacity` animated (compositor-only); all motion layers `absolute` + `pointer-events-none` → no CLS; passive scroll listener + rAF throttle → ~60 fps. Mobile 390px overflow re-verified as zero. Zero copy/route/backend/dependency changes; only file touched is `frontend/components/HomeContent.tsx` (~1346 LOC).
- **Phase 4 — Admin + Clinic Dashboards sky→teal Migration (Feb 19, 2026)** — Site-wide visual unification: bulk regex migration `\bsky-(\d+)\b` → `teal-$1` across all `/admin/**` (16 files, 119 occurrences) and `/clinic/**` (7 files, 87 occurrences) routes + `components/admin/AdminHeader.tsx` + `components/clinic/*` (6 files) + `lib/consultationLabels.ts` (status-badge colors used by both surfaces) + `lib/markdownToHtml.ts` (blog link styling) + `app/review/clinic/[clinicId]/page.tsx` + `app/verify/[token]/page.tsx`. Root admin/clinic pages migrated from `min-h-screen bg-slate-50` → `min-h-screen bg-[#FCFAF8]` (warm ivory base per `/app/design_guidelines.json` Soft-Tech Healthcare archetype). AdminHeader brand `.bg` bumped from `text-teal-500` → `text-teal-600` to match the public `Header` brand wordmark exactly. CookieConsent overlay backdrop now `pointer-events-none` so the visual dim remains but the global Header (`z-50`) stays clickable even before consent — fixes the previous mobile-menu Playwright timeout where the cookie backdrop at `z-[9998]` was intercepting the first hamburger click on fresh sessions. **Zero** copy / route / data-model / backend / dependency changes. **Zero** new TypeScript errors (pre-existing 4 TS errors in `app/admin/dashboard/page.tsx:561` + `lib/api.ts:31` + `lib/articleTestRender.ts:211` + `lib/attribution.ts:241` are unrelated). Mobile sandwich menu functionality VERIFIED (testing_agent_v3_fork iteration_49 confirmed drawer opens at 390×844 with 21+ links including all 15 required SEO labels: Симптоми / Ортодонтия / Импланти / Естетична стоматология / TMJ / Сън и дишане / Криви зъби / Какво е Invisalign / Алайнери vs Брекети / Сравнение на алайнери / Цена Invisalign / Цена импланти / Care Pass / Zubite стандарт / За клиники / Контакти). **Tests**: `testing_agent_v3_fork` iteration_49 → 100% on Phase 4 (all 11 admin+clinic routes + 2 public routes + 4 public smoke routes verified with 0 sky-* class refs, 0 horizontal overflow at 1440 + 768 px, brand teal-600, active-tab `bg-teal-50 text-teal-700`, weekly trend chart teal line, status badges teal+amber). **Backwards compat**: Only side-effect is cookie banner no longer blocks page interactions before consent — banner remains visually present and is still the only path to dismiss the dim layer (still required by privacy policy for "Приеми/Само необходими"). **Untouched per scope**: homepage, quiz, quiz success, clinic profile, blog, static/legal, patient funnel, lead capture, scoring, attribution, SEO, JSON-LD, auth, all backend.
- **Google Analytics 4 + Consent Mode v2 (Feb 19, 2026)** — Privacy-first GA4 install (`G-1EXHPR6JYS`) with full Google Consent Mode v2 wiring. **Architecture**: (1) inline `<script>` in `app/layout.tsx` `<head>` runs FIRST on every page — initialises `window.dataLayer` + `window.gtag`, calls `gtag('consent', 'default', {...})` with `analytics_storage/ad_storage/ad_user_data/ad_personalization = denied` and `functionality_storage/security_storage = granted`, replays the user's stored choice from `localStorage` (`zubite_cookie_consent` + `zubite_cookie_preferences`) as a `gtag('consent', 'update', ...)` so the very first network hit carries the correct signals, then `gtag('js', new Date())` + `gtag('config', GA_ID, { anonymize_ip: true, send_page_view: false })`. (2) External `<script async src="https://www.googletagmanager.com/gtag/js?id=G-1EXHPR6JYS">` rendered in `<head>` directly (NOT via `next/script`, NOT inside any `<Suspense>` boundary — earlier attempts using `next/script` afterInteractive and a Suspense-wrapped client component failed because `useSearchParams()` suspended the component and prevented script injection). (3) New client component `components/analytics/GoogleAnalyticsConsent.tsx` mounted UNSUSPENDED in body provides two runtime bridges: **(a)** listens for `cookie-consent-analytics` + `cookie-consent-marketing` `CustomEvent`s dispatched by `CookieConsent.tsx` and calls `gtag('consent', 'update', ...)` with `granted`/`denied` per the user choice — fires fresh page_view if analytics was just freshly granted; **(b)** `usePathname` effect sends manual `page_view` events on App Router route changes (query string read from `window.location.search` to avoid pulling `useSearchParams()`, which would re-suspend the component). New helpers module `lib/analytics/gtag.ts` centralises `GA_MEASUREMENT_ID`, type-safe `gtag` wrapper, `grantAnalyticsConsent()`, `denyAnalyticsConsent()`, `updateMarketingConsent(granted)`, `trackPageView(url)`, `trackEvent(name, params)`, and `readStoredPreferences()`. Custom events documented but **not yet wired across the app** (`quiz_start`, `quiz_complete`, `lead_submit`, `clinic_profile_view`, `clinic_request_click`, `care_pass_interest`, `article_view` — to be added incrementally per spec). **CookieConsent.tsx updates**: (a) optional categories no longer pre-checked (`analytics: false`, `marketing: false` initial state — per Consent Mode v2 + GDPR best practice); (b) banner now dispatches BOTH `granted` AND `denied` custom events on save (previous code only emitted on grant); (c) hero copy aligned with spec: "Използваме cookies, за да подобрим работата на сайта и да разберем кои страници са най-полезни. Можеш да приемеш всички cookies или да продължиш само с необходимите.". **/cookies page**: added new info box explaining Google Consent Mode v2 — that defaults are denied, GA only fires after user accepts analytics, and choice can be revoked. **Acceptance verified end-to-end via Playwright**: fresh visit → `dataLayer` contains 3 baseline entries (consent default with all PII denied, js Date, config with `send_page_view: false`); GA loader script present exactly **once** (no duplicates); banner visible. Click "Приеми всички" → 2 consent updates pushed (`analytics_storage: granted`, then `ad_*` triplet granted); `localStorage` persists `{necessary:true, analytics:true, marketing:true}`. App Router nav `/` → `/blog` → new `page_view` event fires automatically without reload. Reload after accept → defaults remain denied at boot, BUT inline init replays a single consolidated `consent update` with all 4 signals granted BEFORE the external GA script executes — so the first hit of the new session carries the correct signals. Click "Само необходими" → 2 explicit `denied` updates pushed; `localStorage` persists `{necessary:true, analytics:false, marketing:false}`. Zero TypeScript regressions in touched files. Zero changes to backend / quiz scoring / lead capture / attribution / admin / clinic dashboard / matching / sitemap / robots / auth / data models.
- **GA4 Funnel Events — quiz_start / quiz_complete / lead_submit (Feb 19, 2026)** — Wired the 3 core conversion-funnel events via the existing `lib/analytics/gtag.ts` helper. **All 3 changes live in a single file** (`components/MasterQuiz.tsx`) — zero changes to quiz scoring, lead capture logic, attribution, backend, or other quiz components. **`quiz_start`** fires inside `handleSegmentSelect` (the explicit user-initiated start — Adult/Teen/Child segment selection) — guarded by `gaQuizStartFiredRef` so it fires at most once per quiz session, never on bare `/quiz` page load, never on back-navigation, never on subsequent answers. Payload pulls `utm_source` + `utm_campaign` from `getStoredAttribution().latest` (attribution module already populates this from URL on every visit; falls back to `'unknown'` if no attribution stored). Payload: `{ quiz_type: 'master', entry_path: window.location.pathname, source_context: utm_source||utm_medium||'unknown', utm_source?, utm_campaign? }`. **`quiz_complete`** fires inside the `handleAnswer` calculation branch right after `calculateResult()` succeeds and before `setStep('result')` — guarded by `gaQuizCompleteFiredRef`. Payload: `{ quiz_type: 'master', result_category: band, soonness_band: band, questions_answered: count }` — band is one of `low|moderate|high`. **`lead_submit`** fires inside `handleSubmit` *AFTER* the `if (!response.ok) throw` guard and after `createdLeadId` parsing, but *BEFORE* the `router.push('/quiz/success')` — guarded by `gaLeadSubmitFiredRef`. Critically: validation errors (`return` early on missing fields, invalid phone format, etc.) and API errors (the `throw` branches) never reach the trackEvent call. Payload: `{ quiz_type: 'master', result_category: band, city: city_slug||undefined, lead_type: 'patient_request', source_path: window.location.pathname }` — **NEVER** sends name / phone / email / lead ID / answers / score / free-text symptoms. **Consent**: events ride the existing Consent Mode v2 setup — Google tag drops the hit at network level if `analytics_storage` is still denied; no custom gating inside the component. **E2E verified via Playwright**: fresh `/quiz` load → 0 events; segment select → quiz_start fires once with correct payload; 10 answers + insight screens → quiz_complete fires once with `result_category:'high'`/`questions_answered:10`; form filled + Sofia selected + submit → backend returns 200 + leadId `e8f3f176-...` → lead_submit fires once with `city:'sofia'`. Empty-form submit attempt → validation error shown → 0 lead_submit (confirmed). All 3 GA scripts loaded exactly once (no duplicates). Zero new TypeScript errors. Other quizzes (`OrthodonticsQuiz`, `TreatmentQuiz`, `AlignersVsBracesQuiz`) intentionally untouched — they are educational widgets, do NOT post to `/api/leads`, and are out of the patient-funnel scope per spec. Other patient-funnel events listed in roadmap (`article_view`, `care_pass_interest`, `clinic_profile_view`, `clinic_request_click`) deliberately deferred per task scope: "those can come later".
- **Invisalign България Premium SEO Landing (Feb 19, 2026)** — New public route `/invisalign-bulgaria` (server-rendered, ~660 LOC, single self-contained file at `app/invisalign-bulgaria/page.tsx`) — premium long-form treatment guide page built from user-uploaded Bulgarian content artifact + design spec. **13 designed sections**: (1) Hero with serif H1 "Invisalign в България: цена, лечение и сложни случаи" + 4 trust chips (SmartTrack / официална Invisalign екосистема / ~5 работни дни доставка / Lite-Moderate-Comprehensive) + glass aligner-visual card with 3-tier mini price summary + dual CTAs; (2) Mobile collapsible TOC with 11 anchors; (3) **AEO direct-answer block** — gradient teal card with the canonical 70-word answer to "Работи ли Invisalign при сложни случаи?" plus 4 secondary answer cards (`Колко струва`, `Колко време`, `Какво прави SmartTrack`, `Колко бързо пристигат`) for AI Overviews / ChatGPT / Perplexity extraction; (4) Quick-facts striped table with 9 rows; (5) "Какво е Invisalign?" prose block + dark navy "Не е просто прозрачна пластмаса" call-out; (6) **SmartTrack section** with 2-col layout, 5 bulletted benefits, and SmartTrack-vs-PETG comparison glass card; (7) **Complex cases section** with bold positioning quote in a glass card + 4×2 grid of 8 case types (Струпване / Разстояния / Дълбока захапка / Отворена захапка / Кръстосана захапка / Class II / Екстракционни / Хибридни механики); (8) 8-step timeline as 2-col grid with Lucide icons; (9) **Bulgaria presence** dark navy-teal gradient panel with 3 stat chips (локално представителство / сертифицирани лекари / доставка ~5 работни дни) + footnote "* не е законова гаранция"; (10) **Pricing** — 3 premium price cards (Lite €1.5–2.5k / Moderate €2.5–3.5k featured / Comprehensive €4–5k) with feature lists + 8-bullet "Финалната цена зависи от" panel; (11) Living-with grid (6 daily-life cards: Хранене / Натиск / Говор / Хигиена / Загубен алайнер / Дисциплина); (12) Invisalign-vs-braces 3-col comparison table (7 aspects, balanced wording — does NOT bash braces); (13) FAQ accordion (14 questions covering all spec-required items including refinements, SmartTrack vs cheaper aligners, тийнейджъри, ретайнери); (14) Final CTA glass panel with primary "Провери своя случай за 60 секунди" + secondary "Разгледай как работи Zubite.bg" + 6 internal-link cluster (what-is-invisalign / invisalign-price / aligners-vs-braces / aligners-comparison / crooked-teeth / orthodontics). **SEO + AEO**: full Metadata API config (title, description, 13-keyword string, canonical, OpenGraph article+bg_BG, Twitter summary_large_image), 3 inline JSON-LD blocks (Article schema, FAQPage schema with all 14 Q/A, BreadcrumbList schema Начало › Ортодонтия › Invisalign България), proper H1/H2/H3 hierarchy (1 H1 + 13 H2s), all interactive elements carry `data-testid`. **Sitemap**: added entry priority 0.95, weekly. **Header mobile menu + Footer Guides column** both updated to expose the new page. **Design fidelity**: warm-ivory `#FCFAF8` base, Playfair Display serif H1/H2, teal/cyan radial gradients, glass cards with `bg-white/65 backdrop-blur-xl ring-1 ring-white/70`, dark navy-teal gradient Bulgaria-presence panel matching Care Pass premium aesthetic, teal-gradient CTAs with glossy shine, soft shadows. **Content fidelity**: preserved the strongest copy verbatim from the article — SmartTrack differentiation, Bulgarian Invisalign ecosystem with safe "по обратна връзка от лекари" phrasing for the ~5-day delivery claim, exact price ranges (1500-2500 / 2500-3500 / 4000-5000 €), refinements normalized as routine not failure, balanced positioning ("Invisalign може да лекува много сложни случаи, когато…" — NOT "fixes everything"). **Medical safety**: every section avoids diagnosis claims; Zubite framed as ориентир-not-diagnosis; final CTA copy "Zubite.bg не поставя диагноза" preserved. **Manual TODO**: exact Bulgarian Invisalign legal entity name was intentionally kept generic ("българско юридическо/търговско представителство") per article source — main agent can later swap in the verified entity name in 2 strings inside the Bulgaria-presence section if/when user confirms it (located in JSX: `<strong>българско юридическо/търговско представителство</strong>` × 1 + 1st stat-chip label `Локално представителство`). **Untouched**: backend, DB, auth, admin, clinic dashboards, quiz scoring, lead capture, attribution, blog importer, other SEO landing pages. **Tests**: smoke-screenshot verified — Title + canonical correct, all 13 sections rendered, 3 price cards + 8 timeline steps + 14 FAQ items present, 5 JSON-LD blocks emitted (Organization+WebSite global + Article+FAQPage+BreadcrumbList page-level), **0 horizontal overflow at 1440 and 390 px**, hero visual + trust chips visible above the fold. Zero new TypeScript errors. Mobile 390px tested — fully responsive.
- **Clinic Profile Scheduler — Phase A backend (Feb 2026)** — Public phone-consultation scheduling endpoints on the clinic profile, reusing the existing `online_orientation_bookings` slot engine + state machine — no new scheduling engine, no second booking schema. **Files changed**: `backend/schemas.py` (added `PublicConsultationBookingCreate` + `PUBLIC_CONSULTATION_TYPE_VALUES` constant), `backend/routers/public_clinics.py` (+2 endpoints, +helpers; reuses `orientation_slots.generate_slots_for_clinic`, `orientation_access.get_online_orientation_access_status`, `ORIENTATION_BOOKING_ACTIVE_LOCK_STATUSES`, `clinic_online_orientation_settings` / `_availability` / `_bookings` collections), `backend/tests/test_public_consultation_scheduler.py` (NEW, 13 cases). **`GET /api/public/clinics/{clinic_id}/availability?type=phone_consultation`** returns one of 3 states: `disabled` (no settings doc OR `enabled=False` OR clinic inactive/not in plan), `enabled_no_slots` (settings ok but no availability windows / monthly cap / lock filters all out), `available` (slots[] populated; each slot has `slot_id`, `scheduled_at` UTC ISO, `scheduled_at_local`, `duration_minutes`, `label_local_bg`). 404 for unknown / non-public clinics; 400 for unsupported `type`. **`POST /api/public/consultation-bookings`** rate-limited 10/600s. Validates consent + disclaimer (422), clinic visible (404), scheduler enabled (400), slot ISO parse (422), active-lock free (409). Auto-creates a minimal lead with strict labels: `lead_source="clinic_profile_scheduler"`, `lead_type="phone_consultation_booking"`, `qualification_source="public_profile"`, `is_quiz_qualified=false`, `consultation_type="phone_consultation"`, `clinic_id`, `source_path`. Lead is flagged `contact_details_submitted=true / full_result_unlocked=true / care_pass_eligible=true / care_pass_unlocked=false / consultation_booked_through_zubite=true / band="UNKNOWN"`. Booking row inserted into `online_orientation_bookings` with `status="pending_clinic_confirmation"`, `consultation_type="phone_consultation"`, `topic="not_sure"`, `expires_at = now+24h`, plus mirrored source labels (`lead_source / qualification_source / is_quiz_qualified / source_path`). Response message strictly `"Заявката е изпратена. Клиниката ще потвърди часа."` — never claims confirmed. **Admin / clinic surfaces inherit for free**: the booking lives in `online_orientation_bookings`, so it shows up unchanged in `GET /api/admin/online-orientation-bookings` (admin queue with status + clinic_id filters) and `GET /api/clinic/online-orientation-bookings` (clinic can confirm/reject via the existing `confirm / reject / cancel / mark_completed / …` action endpoint). The new `lead_source` field rides along on the booking doc so admin can identify scheduler-originated traffic. Care Pass invariant preserved: `care_pass_unlocked` stays `false` until the clinic actually confirms (same Phase F unlock rule as quiz-driven leads). **Tests**: `tests/test_public_consultation_scheduler.py` 13/13 PASS (3 states for /availability, 404 unknown, 400 unsupported type, 422 missing consent, 422 missing disclaimer, 400 disabled clinic, 404 unknown POST, happy path with full label validation, 409 double-book, locked slot filtered from re-listing, quiz-flow regression). `tests/test_orientation_bookings.py` 10/10 PASS — confirms no regression on the existing patient-quiz booking flow. **Untouched per Phase A scope**: frontend, clinic profile UI, JSON-LD, quiz flow, demo clinic visibility, scheduling engine, booking state machine, Care Pass unlock logic, admin consultation_requests endpoint. Phase B (frontend `<ConsultationScheduler />`), Phase C (profile redesign), Phase D (analytics events), Phase E (demo availability data) deferred per stop-point instruction.
- **Clinic Profile Scheduler — Phase A patches + Phase B frontend (Feb 2026)** — Two follow-up patches on Phase A backend + Phase B frontend `<ConsultationScheduler />` component. **Phase A patches**: (1) `care_pass_eligible` on the auto-created lead now mirrors the clinic's actual `care_pass_partner` flag instead of hardcoded `True` — clinics that don't run Care Pass never get a Care-Pass-eligible lead (test case 12b added: Care Pass partner → `eligible=True`; non-partner → `eligible=False`); (2) backend accepts whatever canonical `source_path` the frontend sends — Phase B sends the real `/kliniki/[city]/[specialty]/[clinicSlug]` path captured via `window.location.pathname`. **Phase B files**: `frontend/components/public-clinics/ConsultationScheduler.tsx` (NEW, ~510 LOC, single self-contained component with 3 sub-components: `AvailableSlots`, `NoSlotsPanel`, `BookingFormModal`), `frontend/components/public-clinics/ClinicProfileView.tsx` (+13 LOC — adds `useEffect`-based `sourcePath` capture and mounts `<ConsultationScheduler />` right after the hero, above the existing online-consultation request-only section). **Three behavioural states** wired to the Phase A `/availability` response — (1) `state="available"` renders a clean date-grouped slot picker with Bulgarian weekday labels + day chips, Europe/Sofia timezone label, slot-duration footer, disclaimer text, ShieldCheck note "Zubite.bg не потвърждава часове автоматично"; clicking a slot opens a focused booking modal with the chosen slot summary + form (Име*/Телефон*/Имейл optional/Кратко съобщение optional/Consent/Disclaimer-ack/`Изпрати заявка` glossy teal CTA); on success the modal shows the **exact** backend message `"Заявката е изпратена. Клиниката ще потвърди часа."` + reserved-slot summary — NEVER says "часът е запазен" / "потвърден". (2) `state="enabled_no_slots"` renders an amber panel "В момента няма публикувани свободни часове за дистанционна консултация." + glossy teal "Заяви контакт" fallback CTA which opens the existing `PublicContactModal` (POST → `/api/leads`, not scheduler endpoint). (3) `state="disabled"` → component returns `null`; clinic profile continues with the normal "Заяви контакт" CTA unchanged. **POST payload** sent by the modal: `{ clinic_id, scheduled_at, consultation_type: "phone_consultation", name, phone, email?, patient_note?, consent: true, disclaimer_acknowledged: true, source_path: window.location.pathname }`. **Bug fixed mid-session**: original draft refreshed `loadAvailability()` immediately after successful POST, which unmounted the modal and wiped the success state — deferred to the `onClose` handler so success stays visible until "Затвори". `data-testid` inventory: `consultation-scheduler[data-state]`, `scheduler-{loading,error,no-slots,fallback-contact,available,public-description,disclaimer}`, `scheduler-slots-by-day`, `scheduler-day-{YYYY-MM-DD}`, `scheduler-slot-{slot_id}`, `scheduler-booking-{modal,backdrop,back,close,slot-label,error,submit,success,success-message,done-close}`, `scheduler-{name,phone,email,note,consent,disclaimer-ack}`. **Verified E2E via Playwright** at 1280×900 (desktop) and 390×844 (mobile): available with 25 slots/5 days, modal opens with correct slot label, form binds, POST 200 → success rendered with exact backend message, no-slots state on 2nd clinic, disabled state on 3rd clinic (scheduler NOT rendered, standard CTA preserved), 0 horizontal overflow at 390px. **Backend regression**: `test_public_consultation_scheduler.py` 14/14 PASS (Care Pass eligibility added), `test_orientation_bookings.py` 10/10 PASS — quiz-driven booking flow untouched. TypeScript clean. **Untouched per Phase B scope**: quiz flow, JSON-LD, admin calendar/settings logic, scheduling engine, demo clinic visibility, clinic profile redesign (Phase C deferred), GA4 analytics events (Phase D deferred), demo availability data seeding (Phase E deferred).
- **Clinic Profile — Phase C1 tier-aware home redesign (Feb 2026)** — Tier-aware production-safe foundation for the clinic profile as a premium "clinic home", with visible tier-included sections and polished empty states for missing content. NEVER mock data on real clinics. **Backend files**: `backend/schemas.py` (added 7 new `ClinicProfile` enrichment fields: `technology_section`, `expert_qa`, `philosophy`, `faq`, `category_authority`, `price_ranges`, `treatment_details` + 3 new helper models `ClinicProfileQA`, `ClinicProfilePriceRange`, `ClinicProfileTreatmentDetail`; added 3 admin-update flags: `is_sponsored`, `sponsored_label`, `is_addons_showcase`), `backend/routers/public_clinics.py` (`_public_clinic_payload` now exposes every new field with safe defaults — empty lists / null primitives — plus `environment_description`, `team_note`, `clinic_video_url`, `doctor_video_url`, `profile_published_at`, `is_sponsored`, `sponsored_label`, `is_demo`, `is_addons_showcase`; `case_library` filtered to only consent-confirmed published entries), `backend/routers/public.py` (`_is_clinic_visible` now hard-blocks `is_demo === True` unconditionally — demo clinics NEVER reach quiz-driven recommendations regardless of preview env; added `is_demo` to the recommended-clinics projection). **Frontend files**: `frontend/lib/publicClinics.ts` (`PublicClinic` type extended with new fields + tier flags + sponsorship/demo flags), `frontend/components/public-clinics/EmptyStateCard.tsx` (NEW shared polished empty state — premium card with subtle ring, neutral Sparkles glyph, optional `hint` label + body copy "Тази секция е включена в профила, но клиниката все още не е добавила съдържание."), `frontend/components/public-clinics/ConsultationScheduler.tsx` (+`onStateResolved` callback prop so the parent profile view can render a tier-included empty state when the scheduler is disabled; no backend logic change), `frontend/components/public-clinics/ClinicProfileView.tsx` (FULL REWRITE → ~700 LOC tier-gated flowing layout with `<details>`-based accessible accordions, sub-components `SectionShell`, `BadgePill`, `Accordion`, `TreatmentAccordion`, `ContentAttribution`, `DemoBanner`, plus helpers `formatPriceRange` and `consultationStepsFromText`), `frontend/app/kliniki/[city]/[specialty]/[clinicSlug]/page.tsx` (added `robots: { index: false, follow: false }` meta to `generateMetadata` ONLY when `clinic.is_demo === true` — real clinics retain global indexable robots config), `backend/tests/test_profile_phase_c1.py` (NEW, 5 cases). **Tier visibility matrix actually rendered**: Verified (`standard`) → 4 sections only (Hero, Услуги и направления, Локация и контакт, Bottom CTA); Premium (`featured`) → 11 sections (Hero, online-consultation empty if scheduler disabled + flag on, Care Pass real/empty, Suitability, Услуги + per-treatment accordion, Patient approach, Consultation process accordion, Doctor spotlight, Questions checklist accordion, Patient journey "Защо тази клиника се появява тук", Location, Profile trust); Authority (`premium`) → 11 Premium + 7 Authority sections (Deep clinic story + environment + philosophy quote, Technology accordion, Expert Q&A accordion, Real cases accordion, Category authority framing, Price ranges, Content attribution). Empty states render via `<EmptyStateCard />` for every tier-included field that's missing — exact spec copy for cases ("Клиниката все още не е предоставила реални случаи за публикуване."), expert Q&A ("Клиниката все още не е добавила отговори на често задавани пациентски въпроси."), prices ("Клиниката все още не е добавила ценови ориентири. Точната цена зависи от конкретния клиничен случай и се определя след преглед."), and generic ("Тази секция е включена в профила, но клиниката все още не е добавила съдържание."). **Sponsorship**: when `is_sponsored === true`, a separate "Спонсорирано" pill renders next to the tier badge (slate-grey, neutral Megaphone icon, hover-tooltip "Видимостта е спонсорирана. Не влияе на ранкирането."); the literal label is always "Спонсорирано" (frontend constant, not the stored `sponsored_label` — that field is reserved for future per-campaign variations). NO ranking changes, NO matching changes, NO sponsored placement logic. **Demo banner**: sticky amber 100 / amber 900 banner at the very top of `is_demo === true` clinic profiles with copy "Демо профил — примерна визуализация на партньорски функции. Данните не представят реална клиника." **Accordions**: services accordion, consultation-process accordion (parses numbered steps from `consultation_process` text), questions accordion (6 Zubite-supplied neutral questions clearly subtitled "Неутрални предложения от Zubite — не са твърдения на клиниката."), technology / expert-qa / cases accordions for Authority. First item open by default so sections never feel collapsed-blank. **Patient feedback signal**: section gated by `hasRealZubiteFeedback` flag (currently always `false` until we wire real Zubite-owned aggregate). NEVER faked. **Verified Playwright E2E** at 1280×900 + 390×844: Verified Profile demo renders 2 sections only (no Premium empty modules); Premium demo renders all 11 Premium sections incl. online-consultation empty; Authority demo renders 18 sections incl. 6 polished empty states; mobile Authority 0 horizontal overflow at 390px; demo profiles serve `<meta name="robots" content="noindex, nofollow">` while real production pilot clinic (premium tier) serves `index, follow`. Real pilot clinic also tested: 17 Authority sections + 6 polished empty states + working Phase B scheduler with real Sofia-local slots — proving no fake production data was injected. **Backend tests**: `tests/test_profile_phase_c1.py` 5/5 PASS (enrichment field roundtrip, safe defaults, demo exclusion from `recommended_clinics`, demo direct lookup still works in preview, sponsored label gating); `tests/test_public_consultation_scheduler.py` 14/14 PASS unchanged; `tests/test_orientation_bookings.py` 10/10 PASS unchanged. TypeScript clean (`npx tsc --noEmit`). **Untouched per Phase C1 scope**: showcase clinic seed (deferred), listing card add-on chips (deferred), live "Свободни часове" listing chip (deferred — backlog), Growth Insights public block (deferred), admin UI for new fields (deferred), Phase D analytics events (deferred), Phase E demo availability data (deferred), sponsored ranking logic (NEVER), quiz flow scoring, JSON-LD structured data, Care Pass unlock invariants. Phase C2 (showcase clinic + listing card chips) gated on user approval.
- **Clinic Profile — Phase C1.1 layout refinement (Feb 2026)** — Layout-only refinement pass; zero new features. Goal: make the profile feel like a clinic's premium home inside Zubite, not a CMS stack of full-width modules. **Files changed**: `frontend/components/public-clinics/ClinicProfileView.tsx` (FULL REWRITE — compact horizontal hero with side image, 4-card overview grid, horizontal anchor nav, lg+ two-column layout `[1fr,300px]` with right sticky action panel, mobile bottom CTA block; new sub-components `OverviewGrid`/`OverviewCard`/`StickyActionPanel`/`BottomMobileCTA`), `frontend/components/public-clinics/EmptyStateCard.tsx` (compact-by-default variant — tighter padding, smaller glyph, single-line copy), `frontend/components/public-clinics/ConsultationScheduler.tsx` (no backend logic touched; `enabled_no_slots` state now renders as a tight single horizontal card without the giant outer titled section; `NoSlotsPanel` redesigned to be one line with inline icon + body + small CTA). **Hero CTA decision tree** (new): `schedulerState === 'available'` → primary "Запази час за консултация" (smooth-scrolls to the scheduler section anchor) + secondary "Заяви контакт"; `enabled_no_slots` → single primary "Заяви контакт" + small amber note "Клиниката приема заявки за онлайн консултация, но няма публикувани свободни часове."; `disabled` → single "Заяви контакт". **Duplicate "Заяви онлайн консултация" CTA REMOVED** from the hero per spec. **Overview cards** under hero (4-column on lg+, stacks on mobile): "Консултация" (next-step copy adapts to scheduler state, CTA scrolls or opens contact modal), "Подходяща за" (2 bullets preview + link to full section), "Услуги" (4 chip preview + link), "Доверие" (Care Pass + Profile reviewed pills + neutral Zubite line). **Anchor nav**: horizontal scroll bar below overview cards with Обзор/Консултация/Услуги/Подход/Екип/Случаи (Authority only)/Въпроси/Локация — smooth-scroll on click, `scroll-mt-20` on section shells so anchors don't slide behind header. **Right sticky action panel** (lg+ only, `sticky top-20`): primary CTA mirroring hero decision tree, scheduler-state-aware subtitle, secondary "или заяви контакт" when primary is "Запази час…", inline Care Pass mini-card with "Виж как работи →" deep-link, small "Заявката се изпраща към клиниката. Zubite не потвърждава автоматично записване." disclaimer. **Mobile bottom CTA** (lg-only-hidden): single block at the foot of content with the hero primary action. **Care Pass no longer dominates** — large full-width "Zubite Care Pass" section removed entirely; Care Pass surfaces as (a) overview card chip, (b) compact mini-card in sticky panel, (c) deep-link to `/care-pass`. **Empty states everywhere compact**: smaller padding (px-3.5 py-3), light slate-50/60 background, single-line copy, tiny 6×6 glyph — no more hero-like blocks. **Accordions more aggressive**: Услуги per-treatment first item open (only), Process first step open (only), Cases first open (only if present), everything else collapsed by default (Technology, Expert Q&A, Questions checklist, Prices — Prices now itself a top-level accordion). **Visual style** — warm ivory `#FCFAF8`, deep navy text, turquoise accents, rounded `rounded-2xl`/`rounded-xl` cards, soft shadows `shadow-[0_8px_24px_-20px]`, glass `bg-white/65 backdrop-blur-xl`, elegant whitespace, smaller serif title `text-[15px]` per section. **Verified Playwright** at 1440×900 (desktop) + 390×844 (mobile): Authority demo with `schedulerState === "available"` shows compact hero with "Запази час за консултация" + secondary contact CTA, 4 overview cards, anchor nav, two-column layout with sticky right panel containing CTA + scheduler subtitle + Care Pass mini-card; Premium demo with `enabled_no_slots` shows single "Заяви контакт" CTA + amber note + sticky panel mirrors the state; scheduler's `enabled_no_slots` rendering is one compact amber card with inline CTA — no longer a giant section; sticky panel hidden on mobile (lg-block); 0 horizontal overflow at 390px; mobile bottom CTA renders; all empty states are compact single-line cards. **Backend regression**: 14/14 + 10/10 + 5/5 PASS, zero backend changes. TypeScript clean. **Untouched per Phase C1.1 scope**: backend scheduling endpoints, booking state machine, quiz flow, JSON-LD, listing page, showcase seed (Phase C2 still deferred), analytics, admin dashboard, ranking logic, Care Pass invariants. Phase C2 (showcase clinic + listing add-ons) still gated on user approval.
- **Clinic Profile — Phase C2 add-ons showcase clinic (Feb 2026)** — One dedicated visual-add-ons showcase clinic. Sales / QA / demo asset only — never appears in normal patient discovery. **Files changed**: `backend/scripts/seed_addons_showcase.py` (NEW idempotent upsert — `id: demo-clinic-addons-showcase`, `name: "Zubite Демо Клиника — Визуален пакет"`, `is_demo: true`, `is_addons_showcase: true`, `partner_tier: "premium"` → Authority Partner, `is_sponsored: true`, fully populated `clinic_profile.{technology_section, expert_qa, philosophy, faq, category_authority, price_ranges, treatment_details, case_library, doctor_spotlight_*, team_note, clinic_story, environment_description, consultation_process, clinic_video_url, doctor_video_url}` with obviously-demo Bulgarian copy + 2 consent-confirmed demo cases), `backend/routers/public_clinics.py` (+1 line — `is_addons_showcase` ALWAYS excluded from `/api/public/clinics` listings regardless of `ZUBITE_INCLUDE_DEMO_CLINICS` preview gate), `frontend/components/public-clinics/ClinicProfileView.tsx` (added `<ShowcaseListingCardPreview>` section + `<ListingChip>` helper — renders ONLY when `clinic.is_addons_showcase === true`; updated sponsored label helper copy to "Спонсорираната видимост е обозначена отделно и не влияе на органичното подреждане."), `backend/tests/test_addons_showcase_isolation.py` (NEW, 4 cases). **Showcase isolation guaranteed by 4 independent gates**: (1) `_is_clinic_visible` blocks `is_demo` unconditionally → quiz recommendations excluded; (2) `/api/public/clinics` lists now filter `is_addons_showcase != true` unconditionally → listing excluded even in preview env; (3) `sitemap.ts` does not enumerate `/kliniki/*` routes → not indexed via sitemap; (4) `app/kliniki/[city]/[specialty]/[clinicSlug]/page.tsx` injects `robots: { index: false, follow: false, googleBot: { index: false, follow: false } }` for any `is_demo` clinic → noindex,nofollow on the response. Direct URL `/kliniki/sofia/aligners/zubite-demo-klinika-vizualen-paket` resolves only in preview env (`ZUBITE_INCLUDE_DEMO_CLINICS=true`); production lookups return 404. **Listing-card preview section** ("Как изглежда картата в списъка") inside the showcase profile is a self-contained mock that renders WITHOUT importing `PublicClinicCard.tsx` — real listing-card component stays intact. Mock shows: cover image with Authority Partner badge bottom-left + dark-contrasting "Спонсорирано" badge bottom-right (visually separated from tier), 7 add-on chips (Онлайн консултация / Свободни часове in teal-strong / Care Pass / Профил прегледан / Реални случаи / Експертни отговори / Видео представяне), clinic name, city · area, description, disabled "Виж клиниката" CTA + "(визуализация)" marker, footer disclaimer "Add-on chip-овете се показват само когато реални данни ги поддържат. „Спонсорирано“ е винаги визуално отделена от tier badge и не променя органичното подреждане." **Sponsored helper copy** "Спонсорираната видимост е обозначена отделно и не влияе на органичното подреждане." now renders inline below the tier badges on any sponsored profile (showcase or real with `is_sponsored=true`). **Verified Playwright** at 1440×900: showcase top with banner + tier + Спонсорирано + helper line; listing-card preview section; Authority sections (doctor + history + philosophy quote + technology accordion 4 items + expert Q&A 3 items collapsed); cases (1 open + 1 collapsed) + prices accordion + category authority. **Live curl checks**: `/api/public/clinics` returns 4 clinics, showcase id absent; real pilot clinic returns `is_demo:false / is_addons_showcase:false / is_sponsored:false / tier:premium` — UNAFFECTED. **Backend tests**: `test_addons_showcase_isolation.py` 4/4 PASS, `test_profile_phase_c1.py` 5/5 PASS, `test_public_consultation_scheduler.py` 14/14 PASS, `test_orientation_bookings.py` 10/10 PASS. TypeScript clean. **Untouched per Phase C2 scope**: real `PublicClinicCard.tsx`, listing endpoint chip system, live `Свободни часове` chip on real listings, admin UI for new fields, analytics, public sponsored placement / ranking logic, quiz flow, JSON-LD, Care Pass invariants. Phase D (analytics) + Phase E (demo availability data for non-showcase demos) still gated on user approval.
- **Public Header Navigation Cleanup (Feb 2026)** — Refined the global public header per Feb 2026 brief: Zubite.bg is positioned as a dental decision/orientation platform, NOT a clinic directory, and the header should explain that without aggressively pushing the quiz from every page. **Changed nav labels** (both `components/Header.tsx` global header and `components/HomeContent.tsx` embedded homepage `<Nav />`): old `Начало / Симптоми / Ортодонтия / Журнал` (Header.tsx) and old `Какво забелязваш / Как работи / Лечения / Care Pass / Журнал / За клиники` (HomeContent embedded nav) → unified to the **exact 6-item order from the brief**: `Симптоми` (→ `/symptoms`) · `Лечения` (→ `/orthodontics` on cross-page links, `#treatments` on the homepage anchor) · `Care Pass` (→ `/care-pass` cross-page, `#care-pass` anchor on homepage) · `Статии` (→ `/blog`) · `За клиники` (→ `/za-kliniki`) · `Какво е Zubite.bg` (→ `/#kakvo-e-zubite` cross-page, `#kakvo-e-zubite` anchor on homepage). **Removed**: `Клиники` is NEVER present in the main nav (clinic discovery is reached contextually through quiz/result/treatment/symptom/article CTAs — the brief explicitly says `/kliniki` should still exist and use the public header, but the header itself must NOT advertise it). **Removed**: persistent `Започни анализа` desktop CTA (was the dark navy gradient pill in the top-right of both navs). Mobile drawer retains a secondary „Започни анализа" entry — kept inside the drawer per the brief's „no persistent quiz CTA unless it is inside the mobile menu and intentionally secondary" guardrail. **Homepage anchor target** — added `id="kakvo-e-zubite"` to the existing `PatientBenefit` section in `HomeContent.tsx` (line 847) so the nav link is never broken. Re-themed the section's intro block with the brief-approved copy: eyebrow „Какво е Zubite.bg", serif H2 „Платформа за дентална ориентация.", body „Zubite.bg ти помага да разбереш какъв може да е проблемът, какви са възможните следващи стъпки и към какъв тип консултация или клиника да се насочиш. Не поставя диагноза и не замества преглед при стоматолог." + secondary glass in-section CTA „Започни ориентация" → `/quiz` (data-testid `kakvo-section-cta`) — the only quiz CTA outside the mobile drawer, intentionally compact glass-pill not dark gradient. Added `scroll-mt-24` to all in-page anchor sections (`#noticing`, `#how`, `#treatments`, `#care-pass`, `#kakvo-e-zubite`) so the fixed glass header doesn't visually clip the section heading when the patient lands via hash navigation. **Layout fit**: nav row uses `gap-4 lg:gap-5 text-[13px]` and `whitespace-nowrap` so all 6 labels fit cleanly on the `max-w-5xl` rounded glass container without wrapping at 1440 px; mobile shows only logo + hamburger (0px horizontal overflow at 390 px). **Visual continuity preserved** — same liquid-glass container shape (`bg-white/10–15 backdrop-blur-[28px] ring-white/35`), same teal accent on the `.bg` of the Zubite wordmark, same scroll-state intensification, same mobile drawer category groups (Лечения / Ръководства / Цени / Платформа). **Verified live (Playwright 1440×900 + 390×844)**: (1) Homepage desktop — exact nav `['Симптоми','Лечения','Care Pass','Статии','За клиники','Какво е Zubite.bg']`, `nav-cta` count = 0, no standalone „Клиники" item (the only „клиники" substring match is „За клиники"); (2) `/kliniki` desktop — same exact 6-item nav, no persistent CTA — public header is rendered even though the nav does not advertise `/kliniki`; (3) Public clinic profile `/kliniki/sofia/invisalign/[clinicSlug]` — same nav, no CTA; (4) `/care-pass` desktop — same; (5) `/#kakvo-e-zubite` direct navigation — the anchor section renders with eyebrow + serif H2 „Платформа за дентална ориентация." + brief copy + „Започни ориентация" secondary CTA; (6) Mobile homepage + `/kliniki` — 0px horizontal overflow, hamburger menu visible; (7) `/quiz` — simplified internal MasterQuiz header (no public „Симптоми" nav, no persistent quiz CTA from the global Header — its own internal trust pill remains); (8) `/results/[leadId]` — `ResultsHeader` (logo + „Ориентир, не диагноза" pill) unchanged. **TypeScript clean** (3 pre-existing unrelated errors only). **Untouched** per brief: quiz logic, results flow, clinic matching, clinic profiles, scheduler, ranking, Care Pass logic, JSON-LD, SEO/GEO, analytics. **Route mapping verification**: every label routes to an existing page; no broken routes invented. Closest-existing-route policy applied to `Лечения` → `/orthodontics` (the only top-level treatment hub today; once a unified `/treatments` index page lands the label will switch over).
- **Pass B — Full Lead-Context Clinic Profile Unification (Feb 2026)** — Refactored `/results/[leadId]/clinics/[clinicId]/page.tsx` from a 1962-LOC custom layout to a slim 410-LOC wrapper that REUSES the same `ClinicProfileView` component powering the public `/kliniki/[city]/[specialty]/[clinicSlug]` route. Patient journey is now visually IDENTICAL across public and lead-context profile pages — addresses the user's complaint „the design of the clinics profile in results page is not the same as the design inside page /kliniki". **Data flow** (new): (1) `getLead(leadId)` — access gate (locked → `router.replace('/results/[leadId]?notice=locked')`); (2) `getRecommendedClinics(leadId, 3)` — locate clinic by id within the 3-clinic shortlist (preserves the „is this clinic actually in your shortlist?" invariant); (3) `getPublicClinic(slug ?? clinicId)` — fetches full `PublicClinic` payload from `/api/public/clinics/{slugOrId}`; backend helper accepts BOTH slug and id, so the route stays resilient until the slug rollout completes on all partner clinics; (4) `getSelectionState(leadId)` — already-selected banner state. **Wrapper chrome only** (lead context preserved): `ResultsHeader` (logo + „Ориентир, не диагноза" pill), back link → `/results/[leadId]/clinics` (data-testid `profile-back-link`), `RecommendationReasonBanner` (eyebrow `ЗАЩО ВИЖДАШ ТАЗИ КЛИНИКА`, serif H2 „Релевантна за твоята заявка в [City]", `clinic.reason` body, reason chips: „В твоя град" / „Care Pass участваща" / `placement_label`), already-requested emerald banner when `selection.selected_clinic_id === clinicId`. **Public CTAs intact** (per user choice): „Запази час за консултация" + „Заяви контакт" inside `ClinicProfileView` open the public contact/scheduler modals — no `mode` prop added, no callback injection needed. **Public profile body** verified identical between the two routes (same Authority Partner badge, hero image, decision band, dark `#0B1620 → #112832` fit panel, sticky Следваща стъпка sidebar, tabs system, services accordion, technology section, expert Q&A, location, bottom CTA). **Fallback shell** for partner clinics without published public profiles (404 from `/api/public/clinics/{id}`): `ProfileUnavailableShell` shows name + city + calm helper note „Подробният профил на тази клиника все още не е публикуван. Може да заявиш контакт през препоръчания списък" + glass back-to-recommendations button — no fake data injected. **Error states** for clinic-not-in-shortlist / not-found / expired / rate-limited / generic — calm cards with retry + back-to-recommendations CTA. **Verified live (Playwright 1440×900 + 390×844)**: (a) Side-by-side comparison screenshots prove visual parity between `/results/[leadId]/clinics/[clinicId]` and `/kliniki/sofia/invisalign/[slug]` — same hero, badge, decision band, dark sections, sticky sidebar, tabs, sections, footer; (b) Mobile lead-context profile: ResultsHeader stacks, back link + reason banner render above ClinicProfileView body, 0px horizontal overflow; (c) Public `/kliniki` UNTOUCHED — full marketing Header with „Симптоми / Ортодонтия / Журнал" nav + „Започни анализа" CTA (15 occurrences) confirmed; (d) Public clinic profile `/kliniki/.../[clinicSlug]` UNTOUCHED — full marketing Header preserved; (e) Care Pass copy compliance: ZERO violations (`ще ви предостави / ще отключиш / След консултацията получаваш / след посетена консултация през Zubite` all absent across the lead-context profile body, since `ClinicProfileView`'s own CarePassPanel was hardened in Pass A); (f) Backend tests `test_patient_recommended_clinics.py` 28/28 PASS — recommendation ranking / max-3 cap / city-first invariant / demo-showcase exclusion / lead-safe field whitelist UNCHANGED; (g) TypeScript clean — 0 new errors. **Code reduction**: -1552 LOC duplicate clinic profile code deleted from `/results/.../clinicId/page.tsx`; +410 LOC of lead-context wrapper added. Net: −1142 LOC. **Future-compatibility**: `slug` field from Pass A is now consumed by this route. **Untouched** per brief: ranking, JSON-LD, public profile rendering, scheduler backend, booking state machine, quiz flow, lead creation, unlock flow, analytics, max-3 cap, demo/showcase exclusion, public `/kliniki`.
- **Post-Quiz Clinic UX Consistency — Pass A (Feb 2026)** — Safe, P0 visual/copy unification pass between public clinic system and post-quiz lead-context flow per the Feb 2026 brief. **Backend** (`routers/public.py`): added `slug` to `_safe_clinic_payload` + projection on the recommended-clinics endpoint (forward-compatible for future Pass B `ClinicProfileView` reuse; not yet consumed by UI). Test whitelist updated. **Frontend** — NEW `components/ResultsHeader.tsx`: conversion-focused header for `/results/**` routes. Left: Zubite.bg serif wordmark linking home. Right: compact „ОРИЕНТИР, НЕ ДИАГНОЗА" trust pill (teal-700, tracking-[0.18em], hairline teal-100 ring). No public nav, no „Започни анализа" CTA, no mobile menu. Public pages keep the normal `<Header />` — verified `/kliniki` still renders „Начало / Симптоми / Ортодонтия / Журнал" + „Започни анализа". `ResultsHeader` applied to all 3 lead-context routes: `/results/[leadId]`, `/results/[leadId]/clinics`, `/results/[leadId]/clinics/[clinicId]`. **Care Pass copy fix sweep** — eliminated all 4 guarantee/automatic violations in lead-context profile (`app/results/[leadId]/clinics/[clinicId]/page.tsx`): line 632 „След проведена консултация чрез Zubite.bg, клиниката ти предоставя…" → „Care Pass може да се отключи след физическа консултация в участваща партньорска клиника, когато условията са изпълнени. Не е застраховка и не е автоматична отстъпка от лечение."; line 1616 sidebar „След посетена консултация през Zubite.bg клиниката ще ви предостави…" → „Care Pass може да се отключи след физическа консултация в участваща клиника, когато условията са изпълнени."; line 1715 timeline step „След посетена консултация клиниката ви предоставя Zubite Care Pass." → „След физическа консултация в участваща клиника Care Pass може да се отключи, когато условията са изпълнени." + step title „Посещавате консултация" → „Посещавате физическа консултация". `CarePassPanel.tsx` full variant: headline „След консултацията получаваш карта с отстъпки…" → „Отстъпки за продукти за орална хигиена след физическа консултация в участваща клиника."; subtitle „Care Pass се предоставя от партньорската клиника, след като посетиш първоначална консултация, заявена през Zubite.bg." → „Care Pass може да се отключи след физическа консултация в участваща партньорска клиника, когато условията са изпълнени. Не е застраховка и не е автоматична отстъпка от лечение."; benefit chips updated („След проведена консултация" → „След физическа консултация"; „От клиниката" → „От участваща клиника"). **`ClinicRecommendationCard.tsx` visual polish** — moved closer to `PublicClinicCard` system without refactoring: NEW top media strip (h-32 sm:h-36) with `clinic.clinic_profile.hero_image_url` for Premium clinics or teal-gradient placeholder (`bg-gradient-to-br from-teal-100 via-cyan-50 to-white` + Sparkles icon) — matches PublicClinicCard silhouette. Position pill `#01/#02/#03` now overlays top-right of the media strip (was inline). Placement badge („Premium партньор" / „Представена клиника") overlays bottom-left of media strip with the same gradient backdrop trick as PublicClinicCard. Outer card: removed always-on inner-top gloss + inset shadow, switched to PublicClinicCard's lighter `bg-white/80 backdrop-blur-xl ring-1 ring-white/80 shadow-[0_10px_30px_-22px_rgba(15,23,42,0.20)] hover:-translate-y-1` rhythm + `overflow-hidden` so the media strip clips. Content area gets its own `p-5 sm:p-6` flex column. Lead-specific features fully preserved: „В твоя град" chip, conditional Care Pass chip (still gated on `care_pass_partner === true`), „Защо виждаш тази клиника?" expandable disclosure, „Виж профила" primary mint-to-teal CTA, „Заяви контакт" secondary glass CTA, modal request flow, 3-way selection states (isSelected → emerald „Заявката е изпратена", lockedByAssisted → „Вече поискахте помощ от Zubite", hasAnySelection → „Вече избрахте клиника"). **`lib/api.ts`** — `RecommendedClinic` type extended with `slug?: string` for forward compatibility. **Verified live (Playwright 1440×900 + 390×844)**: (1) `/results/[leadId]` + `/clinics` + `/clinics/[clinicId]` — all use ResultsHeader; public „Симптоми" nav absent on lead-context routes; (2) public `/kliniki` retains the full marketing header with „Започни анализа" CTA; (3) cards now have media strip silhouette matching PublicClinicCard (real hero image on Sofia Premium, teal gradient placeholder on the rest); (4) Care Pass chip count verified conditional: 1 chip when only Sofia Premium has `care_pass_partner=true`, 0 chips when none; (5) full Care Pass leak scan on lead-context profile shows ZERO occurrences of „ще ви предостави / ще отключиш / След консултацията получаваш / след посетена консултация през Zubite"; new required phrases („когато условията са изпълнени" / „в участваща" / „физическа консултация") all present; (6) 0px horizontal overflow on /clinics + profile at 390 px; (7) noindex for `/results/**` still emits; (8) `test_patient_recommended_clinics.py` 28/28 PASS; (9) TypeScript clean (3 pre-existing unrelated errors only). **Untouched per brief**: ranking algorithm, max-3 cap, demo/showcase exclusion, JSON-LD, scheduler backend, booking state machine, quiz scoring, lead creation, unlock flow, public `/kliniki` and public clinic profile components. **Pass B added to backlog**: full `ClinicProfileView` unification — refactor `/results/[leadId]/clinics/[clinicId]/page.tsx` (~1962 LOC) to wrap and reuse `ClinicProfileView` (~1678 LOC) with a `mode='lead'` prop carrying leadId + backHref + recommendationReason + selectionState while preserving lead-safe noindex behaviour. Slug field already exposed in the recommended-clinics payload to enable the future fetch via `/api/public/clinics/{slug}`.
- **Post-Quiz Flow Refinement Pass — Friction & Care Pass Wording Fix (Feb 2026)** — QA pass on the previous post-quiz simplification, reducing screen count and tightening Care Pass copy across the patient journey per the Feb 2026 refinement brief. **MasterQuiz** (`components/MasterQuiz.tsx`) — `result-continue-btn` now JUMPS directly from result screen to `form` (city) step, bypassing the redundant `soft_commit` „Искаш ли да видиш какви са опциите?" confirmation screen. The `soft_commit` state remains in the state machine for backward compatibility but is no longer reachable in normal flow (analytics still fires `result_to_soft_commit` with `skip_soft_commit: true` for funnel continuity). CTA copy: „Виж какви са опциите ти" → **„Продължи към опциите"**. Form-step title rewritten from „Последна стъпка преди резултата" → **„Къде търсиш консултация?"**; helper from „Избери в кой град си — данните за връзка ще въведеш на следващия екран…" → **„Използваме града, за да покажем първо релевантни клиники близо до теб."**; submit CTA: „Виж моя резултат" → **„Продължи"**. Band-label softening across all 3 segments: „Нисък риск/Умерен риск/Висок риск" → **„Нисък приоритет / Има сигнали за внимание / Висок приоритет"** (lines 376/383/390/399/406/413/422/429/436 of MasterQuiz.tsx). Same band-label softening applied to `/app/quiz/success/page.tsx` BAND_CONFIG + per-segment summaries (e.g. „Умерен риск — има признаци…" → „Има сигнали за внимание — заслужават професионален поглед.") and `OrientirContent.tsx` BAND_LABELS. **ResultUnlockGate** (`components/patient/ResultUnlockGate.tsx`) — added new `citySlug?: string` prop + `CITY_LABEL` map (sofia/plovdiv/varna/burgas/ruse/stara_zagora/pleven/haskovo). When citySlug is provided, a compact preselected city pill renders above the form fields (`data-testid="result-unlock-city-pill"`) with eyebrow „КЪДЕ ТЪРСИШ КОНСУЛТАЦИЯ", MapPin icon + city name, and helper „Използваме града, за да покажем релевантни клиники близо до теб." — replaces the previous separate city-selection step inside the gate. Value-stack bullets reduced from 5 to 4 with brief-approved copy: drops „Свободни безплатни онлайн часове, когато са налични" (replaced with „Възможност за онлайн ориентация, когато клиниката предлага свободни часове"); drops „Zubite Care Pass след запазена и потвърдена от клиниката консултация през Zubite.bg" (replaced with „Care Pass при участваща клиника след физическа консултация, когато условията са изпълнени"). Care Pass incentive block REWRITTEN to remove the „ще отключиш" promise: new copy „Care Pass може да се отключи след физическа консултация в участваща партньорска клиника, когато условията са изпълнени. Не е застраховка и не е автоматична отстъпка от лечение." Helper line under the value stack: „Безплатните онлайн часове са налични…" → „Онлайн ориентация е налична при избрани партньорски клиники и според свободните им часове." Care Pass clarification under consent UPDATED to brief copy: „Оставянето на контакт или онлайн консултацията не отключват Care Pass автоматично. Care Pass може да се отключи след физическа консултация в участваща партньорска клиника, когато условията са изпълнени." Submit CTA: „Покажи ми резултата" → **„Покажи ми подходящи клиники"**. Parent `/results/[leadId]/page.tsx` now passes `citySlug={lead.city_slug}`. **ClinicRecommendationCard** (`components/patient/ClinicRecommendationCard.tsx`) — secondary CTA copy: „Искам обаждане" → **„Заяви контакт"** (matches the brief's hierarchy: Primary = Виж профила, Secondary = Заяви контакт). **CarePassPanel** (`components/patient/CarePassPanel.tsx`) — both `compact` and `inline` variants UPDATED. Compact: „Отстъпки за продукти за орална хигиена след посетена консултация." → „…след физическа консултация в участваща клиника." + new line „Картата се предоставя от партньорската клиника, когато условията са изпълнени. Не е отстъпка от лечение и не е застраховка." Inline: „…след посетена консултация през Zubite.bg." → „…след физическа консултация в участваща партньорска клиника." **`/care-pass` marketing page** (`app/care-pass/page.tsx`) — 4 surface fixes (reachable from the recommendation page via the "Как работи Care Pass" link): FAQ #1 + #4 + how-it-works subheading + final-CTA helper + footer disclaimer all now explicitly say „**физическа** консултация в **участваща** партньорска клиника" + „когато условията са изпълнени". **Verified live (Playwright 1440×900 + 390×844)**: (1) `/results/[leadId]` locked state — partial teaser + city pill „Пловдив" + new 4-bullet value stack + new Care Pass note (NO „ще отключиш") + new clarification copy + new submit „Покажи ми подходящи клиники"; (2) `/results/[leadId]/clinics` — primary CTA „Виж профила" × 3, secondary „Заяви контакт" × 3, same-city chips × 3, conditional Care Pass chip count = 0 when no clinics participate (verified earlier with toggle = 1); (3) Body-text scans confirm ZERO occurrences of „Умерен риск/Нисък риск/Висок риск", „ще отключиш", or „запазиш онлайн или присъствена" across `/results/[leadId]` + `/results/[leadId]/clinics` + `/care-pass`; (4) Mobile 0px horizontal overflow on both `/results` and `/clinics` at 390 px; (5) `noindex` HTTP header + meta tags still emitted for `/results/**`; (6) Backend `_safe_clinic_payload` + `_score_clinic` ranking untouched (city-first invariant intact); (7) TypeScript clean (0 new errors); (8) `test_patient_recommended_clinics.py` 28/28 PASS. **Deferred to backlog per brief approval**: Fix #7 (simplified conversion header — replacing global `Header` with logo + „Ориентир, не диагноза" pill on `/results/**`) — current global Header still renders; quiz page already uses the simplified pattern, but `/results/**` does not. **Untouched** per brief: ranking algorithm semantics, JSON-LD, public `/kliniki`, scheduler backend, booking state machine, demo/showcase clinic logic, admin dashboards, quiz scoring, lead creation API.
- **Post-Quiz Flow Simplification — Partial Result + Curated Recommendations + Care Pass Reminders (Feb 2026)** — Tightened the post-quiz patient journey per the Feb 2026 brief. New flow: **Quiz → `/results/[leadId]` (partial result + contact gate) → `/results/[leadId]/clinics` (curated max-3 shortlist)**. **Backend files**: `routers/public.py` — `/recommended-clinics` payload now exposes `same_city: True` (always, since `_score_clinic` returns -1 for city mismatch — out-of-city clinics never enter the candidate list) and `care_pass_partner: boolean` (defensive `is True` check). Projection broadened to include `care_pass_partner: 1`. **Test whitelist** (`tests/test_patient_recommended_clinics.py`) updated to allow the 2 new fields + 2 pre-existing fields (`aligner_brands_supported`, `clinic_profile`) — 28/28 PASS. **Frontend files**: NEW `app/results/layout.tsx` server component emitting `metadata.robots = { index: false, follow: false }` — applies noindex/nofollow to ALL nested routes (`/results/[leadId]` + `/results/[leadId]/clinics` + `/results/[leadId]/clinics/[clinicId]`). Verified via curl: `x-robots-tag: noindex, nofollow` HTTP header + `<meta name="robots" content="noindex, nofollow">` + `<meta name="googlebot" content="noindex, nofollow">`. JSON-LD untouched (only global Organization + WebSite emit; zero patient PII). `app/results/[leadId]/page.tsx` REWRITTEN — pre-unlock state shows compact partial-result teaser (eyebrow `ТВОЯТ ОРИЕНТИР` teal-700 `tracking-[0.2em]`, serif H1 `Твоят ориентир: консултация с ортодонт` per segment, per-band explanation, locked Care Pass teaser line) ABOVE `ResultUnlockGate`. Post-unlock state shows full partial result panel (eyebrow `Твоят резултат` chip, serif H1, per-band explanation, **Какво показват отговорите ти** key-findings panel with 1 generic + up to 2 flag-aware bullets derived from `quiz_flags` array — never-diagnostic wording like „насочват към / може да има смисъл / точна оценка изисква преглед"), trust note, and a separate **Next-step card** with eyebrow `СЛЕДВАЩА СТЪПКА` + serif H2 „Виж подходящите клиники за теб" + mint-to-teal CTA `#5eead4 → #2dd4bf → #14b8a6` „Виж подходящи клиники" → `/results/[leadId]/clinics`. Dark Care Pass mini-card on both states with `#0B1620 → #0E1A24 → #112832` gradient (locked `/za-kliniki` token) + glass card inside (`bg-white/[0.06] backdrop-blur-2xl ring-white/15`) + eyebrow `CARE PASS` teal-300. **Locked-redirect notice** surfaces when `?notice=locked` is in URL: amber 100/900 banner „Остави контакт, за да видиш персонализираните препоръки." `handleUnlocked` now redirects directly to `/results/[leadId]/clinics` instead of `/quiz/success` (the latter retained only as fallback code path). `ResultUnlockGate.tsx` — added Care Pass clarification helper inside the consent area: „Оставянето на контакт не отключва Care Pass автоматично. Care Pass може да стане активен след реално проведена физическа консултация в участваща клиника." (data-testid `care-pass-contact-clarification`). `app/results/[leadId]/clinics/page.tsx` — gate access via `useEffect`: fetches `getLead(leadId)`, if `full_result_unlocked !== true || contact_details_submitted !== true` → `router.replace('/results/[leadId]?notice=locked')`. While gate verifies, calm `clinic-match-gate-loading` spinner shown. New page title (Wave-tone serif `text-xl sm:text-2xl`): „Подбрани клиники за твоята заявка" + eyebrow `CLINIC MATCH` teal-700 + new subtitle copy explaining location/relevance/profile-info/Zubite trust signals + 4 trust chips (Ориентир/Насочване/Care Pass след физическа консултация/Без задължение). NEW `care-pass-recommendation-banner` glass card BEFORE the clinic grid: „**Care Pass:** при клиники, които участват, ползите могат да се отключат след физическа консултация. Онлайн разговор или изпратена заявка сами по себе си не активират Care Pass." Added compact public-catalog fallback link „Виж още клиники в публичния каталог" → `/kliniki` below the grid. `ClinicRecommendationCard.tsx` — **Care Pass chip now conditional** on `clinic.care_pass_partner === true` (was always-on); tooltip changed to „Възможни ползи след физическа консултация."; chip label simplified to „Care Pass" (no „след консултация" since the chip's existence already signals participation). New **„В твоя град"** chip prepended when `clinic.same_city === true` (always on /recommended-clinics today). „Защо виждаш тази клиника?" trust list also gates the Care Pass bullet behind `care_pass_partner`. `ClinicMatchEmptyState.tsx` REWRITTEN — Wave glass card with brief-approved copy: „В момента нямаме активна партньорска клиника за избрания град в тази категория." + optional Care Pass helper „Care Pass се показва само при участващи партньорски клиники." + dual CTAs (mint-to-teal `Искам Zubite да ми помогне с насочване` + glass `Виж публичния каталог`). `ConsultationScheduler.tsx` — success state copy extended: „Care Pass може да се отключи само след физическа консултация в участваща клиника." (data-testid `scheduler-booking-success-care-pass-note`). **Verified end-to-end (Feb 2026, Playwright 1440×900 + 390×844)**: (1) **Locked partial result** — eyebrow + serif H1 + RED-band explanation + teaser line all render before unlock gate. (2) **Unlocked partial result** — full panel with 3 findings (1 generic adult + 2 flag-aware from `crowding` + `bite_issue` quiz_flags), next-step CTA mint-to-teal pill, dark Care Pass mini-card. (3) **Locked → /clinics redirect** — direct visit to `/clinics` with `full_result_unlocked=false` bounces to `/results/[id]?notice=locked` with amber notice + unlock gate visible. (4) **Unlocked /clinics page** — title „Подбрани клиники за твоята заявка", Care Pass clarification banner visible above 3 clinic cards, 3 same-city chips, 1 Care Pass chip (only on `care_pass_partner=true` clinic) — conditional logic verified live. (5) **Empty state** — `shumen` lead with `clinic_count=0` shows the Wave glass empty card with Care Pass helper + dual CTAs. (6) **Noindex** — `curl -I` → `x-robots-tag: noindex, nofollow`; HTML head has both `robots` + `googlebot` meta tags. (7) **0px horizontal overflow** at 1440 + 390 px. (8) **Care Pass invariant intact** — `care_pass_unlocked=false` after `/unlock-result` POST (only physical consultation can flip it). **Ranking PROOF**: `_score_clinic` returns -1 for city-mismatch BEFORE any tier boost is added (line 1015-1016 of `routers/public.py`) → out-of-city premium clinic is excluded from the candidate list entirely (never enters `scored.append`). Same-city standard with treatment match (score 150) always beats same-city premium without match (score 108). **TypeScript clean** — 0 new errors (3 pre-existing unrelated). **Backend tests** — `test_patient_recommended_clinics.py` 28/28 PASS; no regression on existing flows. **Untouched per Feb 2026 brief**: ranking algorithm semantics, JSON-LD, public `/kliniki` listing, clinic profile view, scheduler backend, booking state machine, demo/showcase clinic logic, admin / clinic dashboards, quiz scoring, lead creation API, attribution, analytics events.
- **Clinic Profile — `/za-kliniki` brand alignment verification (Feb 2026)** — Visual QA pass confirming `ClinicProfileView.tsx` matches the `/za-kliniki` B2B page's premium token system. Verified live via Playwright at 1440×900 + 390×844 on (a) `demo-clinic-addons-showcase` (Authority Partner + Спонсорирано, full add-on visualisation) and (b) `zubite-demo-authority-partner-clinic` (real Authority profile with compact empty states + no fake data leakage). Confirmed tokens identical to `/app/frontend/components/ForClinicsContent.tsx`: dark section gradient `linear-gradient(135deg, #0B1620 0%, #0E1A24 50%, #112832 100%)` (3 dark sections — Suitability, Care Pass, Final CTA); mint-to-teal pill CTA `linear-gradient(135deg, #5eead4 0%, #2dd4bf 60%, #14b8a6 100%)` ("Заяви контакт" in hero + fit panel + sticky panel + final CTA band); primary solid teal CTA `linear-gradient(135deg, #14b8a6 0%, #0d9488 60%, #0f766e 100%)`; warm-ivory base `#FCFAF8`; teal-200 section eyebrows (СЛЕДВАЩА СТЪПКА / ПОДХОДЯЩОСТ / КОНСУЛТАЦИЯ); Playfair Display serif H1/H2; hairline white/teal accent borders. **Empty-state isolation verified**: real Authority clinic (no `treatment_details` populated) shows compact slate-50 single-line cards ("Все още без разширено описание за това лечение." / "Клиниката още не е добавила обобщение на философията си.") — NEVER injects showcase add-on chips (`Свободни часове` / `Реални случаи` / `Експертни отговори` / `Видео представяне` chips only appear on `is_addons_showcase=true`). 0px horizontal overflow on both desktop (1440px) and mobile (390px). TypeScript clean (`npx tsc --noEmit` — only 3 pre-existing unrelated errors in `app/admin/dashboard/page.tsx`, `lib/api.ts`, `lib/attribution.ts`). No backend / route / data-model / dependency changes in this verification pass.
- **Clinic Profile — Phase C1.2 brand / contrast / motion refinement (Feb 2026)** — Visual refinement only. No new features, no backend changes. Goal: page feels like a premium healthcare-tech microsite, not a CMS stack. **Files changed**: `frontend/components/public-clinics/ClinicProfileView.tsx` (+`useReducedMotion`, +`useActiveSection`, +`<Reveal>` IntersectionObserver wrapper, +`<DarkSectionShell>` navy/midnight variant, +`<BottomCTABand>` final dark CTA, +`<ProgressStep>`, +`<DarkDecisionColumn>`; hero rebuilt with branded gradient wash + turquoise glow + depth shadow + tracking-tight title; anchor nav now sticky-glass with active-section pill highlight; overview "Консултация" card dark navy with turquoise glow; sticky right panel migrated to dark navy glass with teal-gradient CTA + 3-step progress copy; Suitability section converted to dark navy 3-column decision card (Подходяща за / Може да не е идеална / Защо я виждаш); Approach section converted to dark midnight split layout — left clinic narrative + right glass "Първа консултация" card with philosophy quote; bottom CTA band added below location section), `frontend/components/public-clinics/EmptyStateCard.tsx` (+`dark` variant — `bg-white/[0.06]`, ivory text, teal-300 glyph for use inside dark sections). **Motion system**: IntersectionObserver-based scroll-reveal (opacity 0→1 + translateY 12px→0 over 550ms with cubic-bezier ease); `useReducedMotion` hook respects `prefers-reduced-motion: reduce` and disables animation entirely; CSS-only transitions (no new deps like Framer Motion); hover lift on overview cards via `hover:-translate-y-0.5`; chevron rotate on accordion open; turquoise glow micro-rotation on primary CTA icon `group-hover:rotate-[-4deg]`; all animations use `transform` + `opacity` only — no layout-shifting. **Active anchor highlight**: `useActiveSection(ids)` observes each `id` with `rootMargin: '-25% 0px -55% 0px'`; closest-to-top visible id sets active state; nav pill flips to `bg-slate-900 text-white` while passive items stay `text-slate-600 hover:text-teal-700`. **Section rhythm** (top → bottom): light hero → light overview cards (1 dark) → glass sticky anchor nav → **dark decision section** (Suitability) → light services → **dark midnight approach band** → light premium accordions → light scheduler → light questions → light location → **dark navy final CTA band**. **Sticky panel progress copy** (new): `1. Избери час или изпрати заявка → 2. Клиниката потвърждава → 3. Получаваш насока за следваща стъпка` — never claims instant confirmation. **Verified Playwright** at 1440×900 + 390×844: all 5 dark moments render with subtle turquoise glow + teal-300 accents on ivory copy; mobile 0 overflow; active anchor highlight verified ("Услуги" pill highlighted while scrolling through services on mobile); bottom CTA band shows teal-gradient primary "Запази час за консултация" + secondary "или заяви контакт" against navy. **Backend regression**: `test_public_consultation_scheduler.py` 14/14 + `test_orientation_bookings.py` 10/10 + `test_profile_phase_c1.py` 5/5 + `test_addons_showcase_isolation.py` 4/4 PASS. TypeScript clean (`npx tsc --noEmit` zero new errors after fixing one curly-quote JSX literal). **Untouched per C1.2 scope**: backend, scheduler endpoints, booking logic, quiz flow, JSON-LD, listing cards, showcase seed, analytics, admin dashboard, ranking/matching. **Trust copy preserved**: no "best/top" claims, no fake reviews, no diagnosis language, no paid-ranking implication, sponsored helper line intact, "Zubite не определя „най-добра" клиника" repeated as final-line trust note. Phase D (analytics) backlog per user direction.
- **Homepage — Sticky Stacking Value Props (Feb 2026)** — Replaced the 4-card grid in the "Какво е Zubite.bg" section (`PatientBenefit`) with a new `<StackedValueProps />` component (`/app/frontend/components/StackedValueProps.tsx`, ~150 LOC). On `md+` viewports the 4 value-prop tiles (По-малко объркване / По-добри въпроси / По-малко натиск / Допълнителна стойност) use `position: sticky` with progressively larger `top` offsets (5rem → 7.25rem → 9.5rem → 11.75rem) so each card stacks atop the previous as the user scrolls, with the previous card's numbered "01/02/03" header peeking above the next. Alternating editorial palette: card 01 cream `#FCFAF8`, card 02 deep teal `#0F4F4A`, card 03 mint `#E6F4F2`, card 04 deepest navy-teal `#0A2F2D`. Each card features a large decorative serif numeral, eyebrow `01 · ЗАЩО ZUBITE`, icon tile, title, short summary, and longer explanation. The LEFT explainer column ("Платформа за дентална ориентация") is now `lg:sticky lg:top-24` so it remains in view while the right-column cards stack. Mobile (<768px) falls back to a simple vertical `<div className="grid gap-4">` stack — no sticky trap on small screens. **Critical CSS fix discovered & applied during this work**: `app/page.tsx` `<main>` had `overflow-x-hidden`, which silently forces `overflow-y: auto` in browsers because mixing `hidden` on one axis with `visible` on the other is invalid — this made `<main>` a scroll container that broke ALL `position: sticky` descendants. Swapped to `overflow-x-clip` (modern, clips horizontally without creating a scroll context). Same fix applied to the section wrapper (`overflow-hidden` → `overflow-x-clip`). Verified live via Playwright: card sticky positions correctly read 80px / 116px / 152px / 188px at deep scroll, each card's number badge peeks visibly above the next. Copy strictly preserved from the previous benefit grid (no Care Pass policy drift — all 4 clinics universal partner-language retained). Visual identifiers preserved (`data-testid="home-patient-benefit"`); new test ids: `stacked-value-props`, `stacked-benefit-card-{0..3}`. Unused `<details>` benefit cards + `MessagesSquare` import removed from `HomeContent.tsx`. No backend / route / dependency / quiz / Care-Pass-copy changes. Zero new TS / lint issues.
- **Homepage Editorial Pass — Width / Dark Green Section / Horizontal Scroll (Feb 2026)** — Three coupled changes that push the homepage closer to a fashion-editorial feel (inspired by lekuvaisevbulgaria.bg):
  1. **Site-wide container width** — Replaced all `max-w-{5,6}xl mx-auto px-5 sm:px-8` patterns with `max-w-[1600px] mx-auto px-6 sm:px-10 lg:px-16` across `HomeContent.tsx` (12 sections), `Footer.tsx`, `ClinicStandardSection.tsx`, and `StackedValueProps.tsx`. Floating navbar pill intentionally kept narrower (`max-w-5xl`) so it remains a centered "object" rather than a full bar. Result: at 1920px viewport the page now has ~160px margins each side (vs. previous ~384px); at 1440px the content fills with ~64px breathing room.
  2. **"Платформа за дентална ориентация" section → dark brand-green with glow** — The `#kakvo-e-zubite` section is now full-bleed brand teal (`#0F4F4A`). Added layered radial-gradient glow (teal-400 top-left + teal-300 bottom-right + deep navy-teal vignette) plus a subtle 0.06-opacity grain texture so the dark surface never feels flat. Existing parallax blob retained but recoloured to teal-400/15. All typography inside the section inverted to white / teal-200 eyebrow / teal-50/85 body. CTA button switched to solid white-on-dark for max contrast. The `<StackedValueProps inverted />` prop swaps the inner 4 cards to an all-light palette (cream / mint / sand / ice-blue) so they pop off the new dark background — both previous dark cards removed since the section itself now supplies the brand green.
  3. **"Какво се случва след като започнеш?" — horizontal sticky scroll** — Rewrote `HowItWorks` to use the new `<HorizontalSteps />` component (`/app/frontend/components/HorizontalSteps.tsx`, ~150 LOC). Pattern: outer wrapper is `height: ${steps.length * 100}vh` providing vertical scroll distance; a sticky inner container locks to viewport while a horizontally-laid 5-card track translates via `transform: translate3d(x, 0, 0)` driven by scroll progress (computed in a single rAF-throttled scroll listener). Step 05 (Care Pass) keeps its mint accent + badge. A subtle 3px progress bar at the bottom of the sticky frame scrubs left-to-right as the user scrolls. `prefers-reduced-motion` is respected — falls back to calm vertical stack. Mobile (<768px) also falls back to vertical stack — horizontal scrolljacking on small touch screens is hostile. Verified via Playwright: track transform reads `translate3d(-355.785px, 0, 0)` at mid-scroll, no horizontal page overflow, clean transition to next section after scroll completes.
- Section-level overflow audit: kept other sections' `overflow-hidden` untouched since they don't contain sticky descendants; the only `overflow-hidden → overflow-x-clip` switches applied so far remain `<main>`, `#kakvo-e-zubite` and the new `#how` section.
- Imports cleaned: removed `MoveRight` from `HomeContent.tsx` (now lives inside `HorizontalSteps.tsx`).
- Phase B / C / D (Care Pass, /breketi, /treatments, /results, clinic profile, static pages) — pending: same width standard + selective dark-green pattern + overlapping tiles per user request.



## Tech Stack
- **Frontend**: Next.js 14 (App Router), React, Tailwind, shadcn/ui.
- **Backend**: FastAPI + Motor (MongoDB async) + Pydantic v2 + bcrypt + python-jose.
- **Integrations**: Resend (email), ElevenLabs (AI calls), Twilio (telephony), Emergent Object Storage (uploads), Meta Pixel (analytics).

## Key Database Collections
- `leads` — quiz results, contact, assigned_clinic_id, verification_status, scoring band.
- `clinic_applications` — partnership applications, status, notes.
- `clinics` — approved clinic accounts (password_hash, profile).
- `lead_verifications` — 24h verification tokens & responses.
- `blog_posts`, `blog_views`, `analytics_events`, `lead_call_logs`, `uploaded_files`, `admin_users`.

## Security Posture (post-audit Feb 2026)
- JWT_SECRET fail-fast (min 24 chars, ~144 bits).
- CORS strict by default (zubite.bg + preview regex).
- Rate limiting on login/lead/contact/verify endpoints.
- NoSQL injection guards on dict-based body endpoints.
- ElevenLabs webhook strictly enforces HMAC signature with replay protection.
- PII excluded from public lead lookup endpoints.
- Pydantic EmailStr + length validation across input schemas.
- 60-min edit window on `/leads/{id}/contact` to prevent late tampering.

## Roadmap
### Site-wide Visual System Status
- ✅ Phase 1 (Homepage)
- ✅ Phase 2 (Quiz + Results)
- ✅ Phase 2.5 (Clinic List + Clinic Profile)
- ✅ Phase 5 fast-track (Care Pass landing + /za-kliniki B2B)
- ✅ Phase 3 (Blog + Static + SEO landing + patient component sky→teal pass) — Feb 17, 2026
- ✅ Phase 4 (Admin + Clinic Dashboard sky→teal migration + ivory root bg + brand alignment + CookieConsent click-through fix) — Feb 19, 2026

- **Save Care Pass by email (Feb 2026)** — New patient-initiated self-service endpoint `POST /api/leads/{lead_id}/email-care-pass` and frontend modal that emails the patient: their risk-band summary, Care Pass eligibility wording (oral hygiene products only, AFTER consultation), and Manual Recommendation Mode messaging (no instant-match / booking promise). Strict scope and safety: requires explicit `consent_to_email=True` (422 otherwise), Pydantic `EmailStr` validation, 404 on unknown lead, rate-limited to 3 / 5 min / client IP, never leaks phone / quiz answers / attribution back. Helper `send_care_pass_summary_email()` in `emails.py` honors `RESEND_API_KEY=""` by logging & skipping (no patient-visible Resend errors) so preview/dev keeps working. New `SaveCarePassEmailBody` schema. Frontend: new `components/patient/SaveCarePassModal.tsx` with prefilled email (read from `localStorage` via `getStoredLeadContact(leadId)`), consent gate (submit disabled until checked), inline `Loader2` submitting state, success/error states, ESC + backdrop close, full a11y `role="dialog"`. Mounted on `/quiz/success` behind a calm glass CTA `Запази резултата на имейл` directly under the Care Pass strip — only rendered when `leadId` is present in the URL. All copy strictly enforces: no diagnosis language, Care Pass = oral-hygiene discounts AFTER consultation, Zubite team reviews leads manually. New data-testids: `success-save-care-pass-block`, `success-save-care-pass-cta`, `save-care-pass-modal`, `save-care-pass-email-input`, `save-care-pass-consent-checkbox`, `save-care-pass-submit`, `save-care-pass-success`, `save-care-pass-error`, `save-care-pass-close`, `save-care-pass-done`. Tests: `tests/test_save_care_pass_email.py` 4/4 PASS (consent required → 422, unknown lead → 404, invalid email → 422, happy path → 200). Mobile 390×844 verified 0px horizontal overflow. Zero TS errors. Zero changes to quiz, scoring, attribution, admin, clinic dashboards, or other endpoints.

- **Phase D — Online Orientation Settings (Admin + Clinic, tier-gated, June 2026)** — Built the operational foundation that lets Zubite configure Free Online Orientation per clinic before any patient-facing flow exists. Strictly settings + availability only — no booking, no slots, no payment, no video, no calendar sync, no patient UI. **New models** (`backend/schemas.py`): `ClinicOnlineOrientationSettings` (one doc per `clinic_id` in `clinic_online_orientation_settings`) with `enabled`, `addon_enabled_for_basic`, `requires_quiz_completion`, `requires_contact_details`, `monthly_free_slot_limit` (default 10), `slot_duration_minutes` (default 20), `max_bookings_per_day` (default 2), `booking_buffer_minutes` (default 15), `eligible_treatment_categories` (whitelist: orthodontics, aligners, implants, cosmetic_dentistry, general_orientation), `public_description`, `disclaimer_text`, `internal_admin_notes` — all default operationally-OFF so Premium clinics still need explicit admin enablement; and `ClinicOnlineOrientationAvailability` (multi-row in `clinic_online_orientation_availability`) with `day_of_week`, `start_time`, `end_time` (HH:MM 24h), `is_active`. **Derived access helper** (`backend/orientation_access.py`): pure, DB-free `get_online_orientation_access_status(clinic, settings)` returns one of `included_in_plan` / `addon_enabled` / `disabled_by_admin` / `not_available` / `clinic_inactive`. Premium-like tiers (`premium`, `premium_plus`, `founding_premium`) → included-in-plan; basic-like (`standard`, `featured`) → require add-on; `admin_only` → never patient-facing. Inactive sources short-circuit to `clinic_inactive`: `clinic_status ∈ {inactive, probation, waiting_list}`, legacy `status ∈ {paused, inactive, disabled, suspended, expired}`, `subscription_status ∈ {cancelled, unpaid}`, or explicit `is_active=False`. Phase E will branch on this status for patient-facing eligibility. **New router** (`backend/routers/orientation_settings.py`): `GET / PUT /api/admin/clinics/{id}/orientation-settings` (admin-gated, default doc synthesised on first GET — never persisted until PUT), `GET / POST / PATCH / DELETE /api/admin/clinics/{id}/orientation-availability[/{row_id}]` (HH:MM validator + `end_time > start_time` 400 with `code: invalid_time_range`), and `GET /api/clinic/orientation-settings` (clinic-side READ ONLY for the logged-in clinic — strips `internal_admin_notes` + admin-only `reasons`). All admin writes emit audit events: `admin_online_orientation_enabled/_disabled`, `admin_online_orientation_addon_enabled/_disabled`, `admin_online_orientation_settings_updated`, `admin_online_orientation_availability_created/_updated/_deleted`. Registered in `server.py`. **Admin UI** (`frontend/app/admin/clinics/[id]/orientation/page.tsx`): dedicated sub-route, sticky save bar, status banner (Включено в Premium профила / Add-on активиран / Изключено от админа / Add-on функция за Basic профил / Клиниката не е активна), toggles section (`enabled`, `addon_enabled_for_basic` only for Basic, `requires_quiz_completion`, `requires_contact_details`), numeric limits grid, category chip picker, free-text fields (public description / disclaimer / admin notes), weekly availability list with per-row activate/pause/delete + add-row form (day picker + time inputs), access-snapshot debug panel. Linked from the existing admin clinic detail page via `admin-clinic-orientation-link`. **Clinic dashboard read-only card** (`frontend/components/clinic/ClinicOrientationStatusCard.tsx`): mounted on `/clinic/dashboard`, segment-aware copy ("Безплатна онлайн ориентация: включена в Premium профила" / "add-on активиран" / "налична като add-on функция" / "неактивна"), shows monthly limit / duration / max-per-day / active-days summary + categories chips + amber "no windows yet" hint — ZERO inputs / ZERO buttons (truly read-only). Clinic never sees `internal_admin_notes`. **Patient funnel left untouched**: `/quiz`, `/results/[leadId]`, `/quiz/success`, `ResultUnlockGate`, Manual Recommendation Mode are NOT modified. **Tests** (`backend/tests/test_orientation_settings.py`): 5/5 PASS — Premium default→enable (`disabled_by_admin → included_in_plan`), Basic add-on flow (`not_available → disabled_by_admin → addon_enabled`), inactive-clinic short-circuit (`clinic_inactive` even with `enabled=true`), availability CRUD + invalid time-range (400 `invalid_time_range`) + 404 on missing row, invalid category rejected (422). **UI E2E**: admin page renders all banners/sections at 1280×900 and 390×844 (0px horizontal overflow), toggle Enable→Save→banner flips `included_in_plan → disabled_by_admin` and back, clinic dashboard card renders correctly for OrthoBG Варна (Basic + add-on enabled) with 0 inputs/buttons. **Regression**: patient funnel `/quiz → /results/[id] → /quiz/success` still works end-to-end, NO `name=` query param leak, Care Pass card + Manual Recommendation Mode preserved. **TypeScript**: 0 new errors (3 pre-existing unrelated). **No Phase E work**: no patient booking UI, no eligible-clinic cards, no slot locking, no clinic pending list, no Care Pass unlock trigger, no payments.- **Phase H — Lightweight status tracking + notifications (June 2026)** — Scope-change from the original heavy anti-abuse plan: NO patient blocking, NO 90-day no-show block, NO phone/email enforcement, NO dedup based on patient identity, NO patient account requirement. Instead: soft duplicate-prevention + per-status patient notifications + status history. **Soft active-booking guard**: `POST /api/leads/{id}/online-orientation-bookings` returns 409 with code `active_booking_exists` (plus the active booking's `id` and `status`) when the same lead already has a booking in any active-lock status (`pending_clinic_confirmation / confirmed_by_clinic / scheduled`). Bulgarian message exactly per spec: „Вече имаш активна заявка за онлайн ориентация. Можеш да изчакаш потвърждение или да отмениш текущата заявка." Once the previous booking transitions to any terminal status (`rejected_by_clinic`, `cancelled_by_patient/_clinic`, `expired_pending_confirmation`, `completed`, `no_show`, `converted_to_in_clinic`, `not_suitable`) the lead is immediately free to book again — verified by 5 separate rebook-after-X tests. **Status history**: `_append_status_history(...)` helper appends `{previous_status, new_status, action, actor_type, actor_id, note, created_at}` to `booking.status_history` on EVERY transition — clinic / admin / patient cancel / lazy expiry / `add_note`. Admin GET response now includes `status_history` (and `notifications_sent`); clinic / patient responses still strip them. **Patient notifications** (`_send_patient_status_email` rewritten): mapped Bulgarian subject + body for each of the 9 outcome statuses (`confirmed_by_clinic`, `rejected_by_clinic`, `cancelled_by_clinic`, `cancelled_by_patient`, `expired_pending_confirmation`, `completed`, `no_show`, `converted_to_in_clinic`, `not_suitable`). Idempotent via a `notifications_sent` array on the booking doc (each status appears at most once). Best-effort: gracefully skips when `RESEND_API_KEY` is unset and never blocks the request on email failure. Audit events `booking_status_notification_sent` / `booking_status_notification_failed` registered in `audit.ACTION_KEYS`. **Lazy expiry rewritten** (`_expire_and_notify`): wraps `expire_pending_bookings` so that, for each booking it just expired, it appends a `lazy_expire` history row and dispatches the patient email. Replaces every read-side call in the bookings router. **Clinic UI**: pending-row helper text "След потвърждение пациентът ще отключи Zubite Care Pass." already in place from Phase F; confirmed-row additional helper: „Завърши, „не се яви", „премина в клиниката" и „не е подходяща" са само статусни маркировки — изпращат уведомление до пациента, но НЕ блокират бъдещи заявки." **Admin UI**: per-row collapsible "История на статуса (N)" `<details>` block showing `created_at · previous → new (от actor · action) [note]`, plus tiny "Изпратени уведомления: N" line. Patient-side duplicate state surfaces verbatim via the existing `booking-error` slot in the modal (verified visually). **Rate-limit bump**: `orient_book_create` 6/600 → 30/600 to support the test suite's rebook cycles without hitting the limit during normal patient retries. **Tests** (`backend/tests/test_phase_h_status_tracking.py`): 9/9 PASS — duplicate active blocked → rebook after reject → rebook after cancel_by_clinic → rebook after expiry → rebook after no_show without any blocking field set → history rows recorded → all 4 terminal status actions usable (mark_completed/no_show/converted_to_in_clinic/not_suitable) → lazy expiry only touches pending bookings → `/api/leads/{id}` still hides phone/email/consent. Phase D (5/5), Phase E (10/10), Phase F (8/8) tests all still PASS after backend restart. **UI E2E** (1280×900 + 390×844, 0px overflow): patient submits booking → page reload → second-booking attempt → modal shows exact active-booking error copy. **No Phase G code**: no reminders, no GA dashboards. No payments / video / uploads / external calendar sync. Manual Recommendation Mode preserved. TypeScript: 0 new errors.

 — Centralised Care Pass unlock helper + wired both confirm paths. **New** (`backend/care_pass.py`): `unlock_care_pass_for_lead(lead_id, *, consultation_type, clinic_id, source_booking_id|source_request_id, actor_type, actor_id, request)` — the single source of truth. Guards: 404-equivalent failure code `missing_lead`, 422-equivalent `missing_contact_details` (lead.contact_details_submitted must be True), idempotent `already_unlocked` short-circuit that preserves the original `care_pass_unlocked_at`. On first unlock atomically sets `consultation_booked_through_zubite=True`, `clinic_confirmed_consultation=True`, `clinic_confirmed_consultation_at`, `consultation_type`, `care_pass_eligible=True`, `care_pass_unlocked=True`, `care_pass_unlocked_at`, `care_pass_source` (`online_orientation_booking` / `consultation_request`), `care_pass_source_id`. Best-effort patient email ("Zubite Care Pass е отключен") + admin email ("Care Pass отключен след потвърдена консултация"). Audit events: `care_pass_unlocked` / `care_pass_unlock_skipped_already_unlocked` / `care_pass_unlock_failed_missing_lead` / `care_pass_unlock_failed_missing_contact_details` (all four registered in `audit.ACTION_KEYS`, alongside the Phase D/E events added in this batch). **Wired**: (1) Phase E `POST /clinic/online-orientation-bookings/{id}/action {action:'confirm'}` and the admin-override equivalent — both call the helper with `consultation_type='online_orientation'`, `source_booking_id`; (2) existing offline consultation `POST /api/clinic/consultation-requests/{id}/action {action_type:'book_consultation'}` — calls the helper with `consultation_type='offline_in_person'`, `source_request_id`. Reject / cancel / expire / no-show paths do NOT unlock (verified by test). **Patient UI** (`frontend/components/patient/CarePassLiveStatus.tsx`): tiny live badge under the existing Care Pass strip on `/quiz/success`. Fetches `GET /api/leads/{id}` on mount + on `visibilitychange` so a patient who leaves the tab open sees the unlocked state without a manual refresh. Three states: hidden (not yet eligible), amber `care-pass-eligible-locked` ("Care Pass ще се отключи след потвърдена от клиниката онлайн или присъствена консултация."), emerald `care-pass-unlocked` ("Zubite Care Pass е отключен. Вече имаш достъп до партньорски предложения и отстъпки за продукти за орална хигиена."). **Clinic UI**: added a `[11px] text-slate-500` helper line under the Confirm/Reject row on `/clinic/dashboard/online-orientation` — "След потвърждение пациентът ще отключи Zubite Care Pass." (`data-testid="care-pass-helper-{id}"`), only on pending rows. **Tests** (`backend/tests/test_care_pass_unlock.py`): 8/8 PASS — (1) booking creation does NOT unlock; (2) clinic confirm unlocks (all flags + source fields); (3) repeated confirm idempotent (timestamp preserved); (4) reject does NOT unlock; (5) lazy-expired booking does NOT unlock; (6) locked/no-contact lead → `failed:missing_contact_details`; (7) missing lead → `failed:missing_lead`; (8) helper direct-call idempotency returns `already_unlocked` with the original timestamp. Phase D (5/5) + Phase E (10/10) tests still PASS after backend restart. **UI E2E** at 1280×900 + 390×844 (0px overflow): `/quiz → /results/[id] → /quiz/success` shows amber `care-pass-eligible-locked` badge; patient submits booking; admin/clinic POSTs `{action:'confirm'}`; patient page re-fetches on `visibilitychange` and flips to emerald `care-pass-unlocked` badge. **No Phase G/H code**: no reminders, no GA dashboards, no 90-day block, no dedup, no payments, no video, no uploads, no external calendar sync. Manual Recommendation Mode untouched. TypeScript: 0 new errors.

 — Built the patient-booking layer on top of Phase D settings. Strict scope: booking requests + clinic confirmation actions + admin overview. **NO Care Pass unlock** (Phase F), NO payments, NO video calls, NO uploads, NO external calendar sync. Patient funnel placement: a new `<FreeOnlineOrientationSection leadId={leadId} />` block on `/quiz/success` (rendered only when `leadId` is present in the URL — same gate as the Save-Care-Pass block). Manual Recommendation Mode is preserved above. **New schemas** (`backend/schemas.py`): `OnlineOrientationBookingCreate` (patient body — NO name/phone/email, server reads from lead), `OnlineOrientationBookingClinicAction`, `OnlineOrientationBookingAdminAction`, `ORIENTATION_BOOKING_STATUS_VALUES` (12 statuses including `pending_clinic_confirmation`, `confirmed_by_clinic`, `rejected_by_clinic`, `expired_pending_confirmation`, `scheduled`, `completed`, `no_show`, `cancelled_by_patient/_clinic`, `converted_to_in_clinic`, `not_suitable`, `needs_admin_review`), `ORIENTATION_BOOKING_ACTIVE_LOCK_STATUSES` (pending / confirmed / scheduled), `ORIENTATION_TOPIC_VALUES` (`aligners_braces / implants / cosmetic / gums_periodontology / not_sure / other`), Bulgarian label map. **Slot generator** (`backend/orientation_slots.py`): pure helper that, given a clinic + Phase D settings + availability rows, returns up to 18 slots over the next 14 days in Europe/Sofia tz with `MIN_NOTICE_HOURS=2`. Filters out: past slots, sub-notice slots, slots overlapping with active-lock bookings, slots that would exceed `max_bookings_per_day`, slots that would exceed `monthly_free_slot_limit`. Each slot carries a Bulgarian display label (`Сряда, 12 юни · 09:00`). Also exports `expire_pending_bookings(db, clinic_id?)` for lazy expiry on every read. **New router** (`backend/routers/orientation_bookings.py`): `GET /api/leads/{lead_id}/eligible-orientation-clinics` (PATIENT — gated on `contact_details_submitted` + `full_result_unlocked`, returns 403 `lead_not_unlocked` otherwise; conservative MVP eligibility = clinic must have `general_orientation` in its category whitelist + Phase D access status ∈ `included_in_plan / addon_enabled`; never exposes phone/email or admin reasons), `POST /api/leads/{lead_id}/online-orientation-bookings` (PATIENT — rate-limited 6/600s; requires `consent_confirmed=True` + `disclaimer_acknowledged=True`, returns 422 `consent_required` / `disclaimer_required`; re-validates slot is still unlocked with 409 `slot_already_locked` to prevent races; sets `lead.consultation_booked_through_zubite=True` and `lead.consultation_type='online_orientation'` but explicitly does NOT touch `care_pass_unlocked`; PII pulled server-side from lead, never accepted in body; 24h `expires_at`; best-effort patient/clinic/admin emails via `_send_email` which gracefully skips if `RESEND_API_KEY` is unset), `POST /api/leads/{lead_id}/online-orientation-bookings/{id}/cancel` (PATIENT cancel), `GET /api/clinic/online-orientation-bookings` + `POST /api/clinic/online-orientation-bookings/{id}/action` (CLINIC — confirm / reject / cancel / mark_completed / mark_no_show / mark_converted_to_in_clinic / mark_not_suitable / add_note; clinic-isolation enforced via `clinic_id` filter; PII visible to the owning clinic only; audit events `orientation_booking_{action}`), `GET /api/admin/online-orientation-bookings` + `POST /api/admin/online-orientation-bookings/{id}/action` (ADMIN — status+clinic filters; manual `release_slot=True` for stuck pendings; audit events `admin_orientation_booking_{action}`). Lazy expiry runs on every read endpoint (`expire_pending_bookings`) — pending bookings past `expires_at` flip to `expired_pending_confirmation` with `expired_at` set, releasing their slot. **Patient UI** (`frontend/components/patient/FreeOnlineOrientationSection.tsx`): section title "Безплатна онлайн ориентация" + intro + amber disclaimer, per-clinic accordion cards with up to 8 slot chips per clinic, "Заяви онлайн час" CTA, `BookingRequestModal` with topic dropdown + consent + disclaimer checkboxes + `Изпрати заявка` submit (rate-limit error surfaced verbatim), success card "Заявката за онлайн час е изпратена. Клиниката ще я прегледа и потвърди.". Empty state: "В момента няма свободни онлайн часове при партньорска клиника за този тип случай." GA4 events fired: `free_orientation_section_viewed` / `free_orientation_clinic_card_viewed` / `free_orientation_booking_started` / `free_orientation_booking_request_submitted`. **Clinic dashboard** (`frontend/app/clinic/dashboard/online-orientation/page.tsx`): 3-group view (Чакат потвърждение / Потвърдени / История) with full per-card actions, internal-note textarea, PII visible only to the owning clinic. Linked from `ClinicShell` sidebar as "Онлайн ориентация". **Admin** (`frontend/app/admin/online-orientation-bookings/page.tsx`): full-platform overview with status + clinic-id filters, manual confirm/reject/cancel(release_slot)/add-note actions. Linked from `AdminHeader` nav as "Онлайн заявки". **Tests** (`backend/tests/test_orientation_bookings.py`): 10/10 PASS — eligible-clinics filtering (Premium enabled + Basic w/ add-on shown; Basic w/o add-on, disabled, inactive hidden); consent_required 422; happy POST creates `pending_clinic_confirmation` and `care_pass_unlocked` stays False; second booking on the same slot returns 409 `slot_already_locked`; locked slot disappears from re-listing; admin list returns the booking; lazy expiry flips `pending → expired_pending_confirmation` when `expires_at` backdated; expired slot re-enters the pool; patient cancel works; locked lead returns 403 `lead_not_unlocked`. **UI E2E** (1280×900 + 390×844, 0px overflow): patient `/quiz → /results/[id] → /quiz/success` shows the section, opens accordion, selects slot, submits modal, sees pending copy. Patient funnel files **untouched** functionally; only `/quiz/success` got a new section import + render. **Regression**: Phase B + Phase D tests still PASS after Phase E backend restart. **No Phase F/G/H code**: Care Pass remains LOCKED throughout (verified by direct DB check in `step3 PASS` of the booking test — `care_pass_unlocked is False` after a successful booking).

 — Three small edits to eliminate even the first-word name leak into the URL after the unlock gate. `frontend/app/results/[leadId]/page.tsx` — `handleUnlocked` no longer appends `name` to the redirect URL (now: `/quiz/success?leadId=...&stage=...&city=...&segment=...`). `frontend/app/quiz/success/page.tsx` — removed `const name = searchParams.get('name')` + the `, ${name}` interpolation in the H1 (greeting now uses the band-specific generic line, e.g. „Благодарим ти!"). `SaveCarePassModal` no longer receives `defaultName`. No backend changes. Verified end-to-end on desktop (1280×800) and mobile (390×844): `0px` horizontal overflow, generic greeting in DOM, Manual Recommendation Mode + Care Pass card still rendered (3 Care Pass mentions). TypeScript: 0 new errors. `GET /api/leads/{id}` still does not expose `phone/email/consent`.

- **`/breketi` SEO/GEO landing V1 (Feb 2026)** — New patient-facing orientation page at `frontend/app/breketi/page.tsx` (~720 LOC, self-contained server component, 0 client JS). **12 sections**: hero (breadcrumb Начало→Лечения→Брекети + H1 "Брекети — кога могат да са подходящи и какво да обмислиш" + 3 trust chips + dual CTA `/quiz` primary + `/aligners-vs-braces` secondary), "Какво представляват" glass card, "Кога могат да са подходящи" (6 safer-phrased bullets including "по-сложни ортодонтски случаи" and "при деца или тийнейджъри след препоръка за ортодонтска оценка"), **decision table** (responsive: desktop `<table>` + mobile stacked cards, 7 criteria — Видимост, Дисциплина, Сложни случаи, Хигиена, Ограничения в храната, Ориентировъчна цена, Брой посещения), **видове брекети** 4 cards (метални, керамични, лингвални, само-лигиращи — each with caveat "Какъв тип е подходящ за случая ти зависи от преглед и план на ортодонт"), **price factors** (6 bullets) + **price strip** using **only** `TREATMENT_PRICES['orthodontics-braces']` (€1,000-€4,000 / 2,000-8,000 BGN — no new ranges; aligners price in decision table also pulled from approved `TREATMENT_PRICES['orthodontics-aligners']`), "Кога да говориш с ортодонт" (5 safe signals + disclaimer "Точна оценка изисква преглед и при нужда образна диагностика"), **8 въпроса към ортодонт** in native `<details>` (each with "Защо този въпрос е важен" framing — Zubite never answers clinically as if it were the orthodontist), Care Pass compact strip with literal compliant copy + "Не е застраховка и не е автоматична отстъпка от лечение" + `/care-pass` link, **7 FAQ** entries native `<details>` (възраст, продължителност, болка, смяна тип, брекети vs алайнери, ретейнер, "Zubite поставя ли диагноза"), **related materials** 4 cards (`/aligners-vs-braces`, `/orthodontics`, `/crooked-teeth`, `/treatments`), final CTA orientation-first (`Започни ориентация` → `/quiz` primary, `Виж партньорски клиники за ортодонтия в София` → `/kliniki/sofia/ortodontia` secondary lighter). **3 JSON-LD blocks**: `MedicalWebPage` (audience: Patient, lastReviewed, isPartOf WebSite, description starts "Информацията е ориентировъчна и не замества преглед при ортодонт"), `BreadcrumbList` (3-level), `FAQPage` (7 Q/A). Schema deliberately excludes `Service`, `MedicalProcedure`, `Offer`, `Dentist`, `Product`, `PriceSpecification` — **all 6 verified absent** in rendered HTML. **Metadata**: title "Брекети — ориентир за пациента: видове, цени, консултация | Zubite.bg", canonical `https://zubite.bg/breketi`, OG/Twitter with explicit safe orientation framing (overrides root-layout fallback). **Sitemap**: `app/sitemap.ts` `staticPages` += `/breketi` (weekly, priority 0.85); total URLs 65 → **66**. **`/treatments` update**: `breketi` promoted from `SOON_CATEGORIES` → `LIVE_CATEGORIES`; `treatment-card-breketi` is now an active link to `/breketi` with secondary "Сравнение с алайнери" → `/aligners-vs-braces`; `treatment-card-soon-breketi` removed; ItemList JSON-LD now contains **7 LIVE categories** (Ортодонтия, Алайнери, Импланти, Естетична стоматология, TMJ, Сън и дишане, Брекети). **Copy guardrails verified**: 0 hits for "най-добро лечение" / "гарантирано" / "това е твоето лечение" / "най-добри ортодонти" / "топ клиники" / "имаш диагноз" / "диагнозата е" in `/breketi` rendered HTML. Care Pass compliance: literal compliant copy + explicit clarification "Не е застраховка и не е автоматична отстъпка от лечение". **Validation**: `/breketi`=200, `/treatments`=200 (with breketi now LIVE), `/`=200, `/quiz`=200, `/results/[id]`=200 with noindex preserved, header untouched (`nav-лечения` → `/treatments`), 4 screenshots captured (desktop hero, desktop decision table, mobile hero, mobile types grid), 0 horizontal overflow at 1440/768/390/375 px, TypeScript 0 new errors (4 pre-existing unrelated). **data-testid** coverage: `breketi-page`, `breketi-hero`, `breketi-hero-primary-cta`, `breketi-hero-secondary-cta`, `breketi-what`, `breketi-when`, `breketi-decision-table`, `breketi-decision-table-cta`, `breketi-types`, `breketi-type-{metalni,keramichni,lingvalni,samo-ligirashti}`, `breketi-price-factors`, `breketi-price-strip`, `breketi-when-to-talk`, `breketi-questions`, `breketi-care-pass-strip`, `breketi-care-pass-link`, `breketi-faq`, `breketi-related`, `breketi-final-cta`, `breketi-final-cta-primary`, `breketi-final-cta-secondary`. **Out of scope**: `/ortodont/[city]`, analytics, admin UI, canonical refactor, clinic data — all untouched. Files: NEW `frontend/app/breketi/page.tsx`; EDIT `frontend/app/sitemap.ts` (+1 line); EDIT `frontend/app/treatments/page.tsx` (promote breketi from SOON to LIVE).

- **Care Pass standardization — universal benefit for every Zubite patient (Feb 2026)** — Major positioning shift. Care Pass is **no longer conditional**: every partner clinic now provides Care Pass to every Zubite patient as a standard benefit. All "may unlock / when conditions are met / after physical consultation" framing replaced with: **"Care Pass е включен в партньорската ни мрежа. Всеки Zubite пациент получава Care Pass при посещение в партньорска клиника. Не е застраховка и не е автоматична отстъпка от лечение."** Replacements across 11 files: `app/care-pass/page.tsx` (title `Zubite Care Pass за всеки наш пациент`, hero H1 `Care Pass — за всеки наш пациент в партньорска клиника`, OG/Twitter/canonical, trust chips, FAQ Q0/Q3/Q4, "Как работи" subhead, final CTA, footnote disclaimer), `app/treatments/page.tsx`, `app/breketi/page.tsx`, `app/results/[leadId]/page.tsx`, `components/patient/CarePassPanel.tsx` (hero+compact+inline variants; chips changed `След физическа консултация` → `Включен за всеки наш пациент`), `components/patient/ResultUnlockGate.tsx`, `components/public-clinics/ConsultationScheduler.tsx`, `components/patient/ClinicStandardSection.tsx`, `components/HomeContent.tsx`. **Filter removal** in `PublicClinicFilters.tsx`: "Care Pass партньор" chip filter retired; URL key tolerated on read for backwards compat but never written; rendered HTML now exposes only `filter-online_consultation`, `filter-accepts_adults`, `filter-accepts_children` + city/specialty selects. **Care Pass chips retained on every partner card** as positive reassurance (since every active partner is `care_pass_partner=true`). **`CarePassLiveStatus`** refactored: retired the "locked" UI (`Lock` icon + "ще се отключи след…"). New states: (1) `unlocked=true` → green "Care Pass е отключен"; (2) anything else → teal `Gift` badge "Zubite Care Pass те очаква при посещението ти в партньорска клиника." `data-testid="care-pass-eligible-locked"` removed; `care-pass-awaiting-visit` added. **DB schema untouched**: `care_pass_partner` boolean + lead-side `care_pass_eligible/unlocked` kept (per user choices 4a + 5b). **Sweep** confirmed: 0 remaining hits for the old conditional language in rendered HTML. **Validation**: TypeScript 0 new errors; `/care-pass`/`/treatments`/`/breketi`/`/kliniki`/`/results/[id]` all 200; `filter-care_pass` absent from `/kliniki`; 0 horizontal overflow at 1440/768/390/375 px on `/care-pass`; screenshots verified new H1 and positive trust chips. **Out of scope** (per direction): backend `care_pass_partner` enforcement; admin UI care_pass toggle cleanup. Files: 11 modified.

- **Homepage → `/treatments` secondary link (Feb 2026)** — Added a single visually-quiet inline link under the homepage `TreatmentCategories` grid: `Виж всички категории →` (`data-testid="home-treatments-see-all"`, href `/treatments`, teal text, small ArrowRight icon, centered `mt-8 sm:mt-10`). No new section, no layout-rhythm changes, no new CTAs elsewhere. Reuses the existing `Reveal` animation pattern with `delay={cats.length * 60}` so it fades in after the 8 category cards. Validation: link rendered at `/`, homepage HTTP 200, `/treatments` HTTP 200, header `Лечения` still → `/treatments`, 0 horizontal overflow at 1440/768/390/375 px on homepage, TypeScript 0 new errors. Files: `frontend/components/HomeContent.tsx` only (+15 lines, 1 location, no other edits).

- **`/treatments` hub V1 — Лечения orientation page (Feb 2026)** — New self-contained server-component page at `frontend/app/treatments/page.tsx` (~480 LOC, no client JS). Sections: hero (H1 "Какъв тип лечение или консултация може да е релевантна за теб?" + 3 trust chips + dual CTA → `/quiz` primary, `/kliniki` secondary lighter style), intro micro-section glass card, **categories grid** with 6 LIVE cards (Ортодонтия→`/orthodontics`, Алайнери→`/aligners-vs-braces`, Импланти→`/implants`, Естетична стоматология→`/cosmetic-dentistry`, Челюстна става / TMJ→`/tmj`, Сън и дишане→`/sleep-airway`; orthodontics + aligners featured with teal ring) and 3 SOON cards (Брекети with secondary→`/aligners-vs-braces`, Венци и орална хигиена with secondary→`/symptoms`, Детска стоматология no link — each with `Скоро` badge + inactive visual state, no primary route). Dark navy "Не си сигурен/а откъде да започнеш?" panel with `/quiz` + `/symptoms` CTAs. Care Pass compact strip with **single** mention using the exact compliant copy + `Виж как работи Care Pass` → `/care-pass`. FAQ accordion with 6 native `<details>` (Zubite не поставя диагноза, как избираш категория, разлика категория vs процедура, кога посещаваш лекар, как разглеждаш клиники, как работи Care Pass). Final CTA = orientation-only (Започни ориентация → `/quiz`), no second `/kliniki` push. **3 JSON-LD blocks**: `CollectionPage` (id, url, name, inLanguage bg-BG, description "не заменя преглед", isPartOf WebSite), `BreadcrumbList` (Начало → Лечения), `ItemList` of **only the 6 LIVE categories** with absolute URLs. Schema deliberately excludes `Service`, `MedicalProcedure`, `Dentist`, `Offer` and soon-card URLs. **Metadata**: title "Лечения и категории · Дентална ориентация | Zubite.bg", canonical `https://zubite.bg/treatments`, OG + Twitter aligned. **Sitemap**: `app/sitemap.ts` `staticPages` += `/treatments` (changeFrequency weekly, priority 0.85); total URLs 64 → **65**. **Header**: `components/Header.tsx` line 36 `Лечения` link now points `/treatments` (was `/orthodontics`); mobile drawer "Лечения" group with deep-links to individual treatment pages remains unchanged; no `Клиники` re-added; no persistent `Започни анализа` header CTA re-added. **Copy guardrails**: zero hits for "най-добро лечение" / "гарантирано решение" / "точното лечение" / "диагнозата е" / "имаш диагноз" anywhere in rendered HTML; all category copy uses "може да е релевантно" / "ориентир" / "обсъди с лекар" framing. **Care Pass**: single literal mention of "Care Pass може да се отключи след физическа консултация в участваща партньорска клиника, когато условията са изпълнени." plus FAQ clarification ("Не е застраховка и не е автоматична отстъпка от лечение"). **Validation**: `/treatments`=200, `/orthodontics`=200 (untouched), `/quiz`=200 (untouched), `/results/*`=200 with `noindex` preserved, sitemap=65 URLs incl. `/treatments`, JSON-LD 3 blocks emit cleanly (8 ListItem total = 2 Breadcrumb + 6 ItemList), 0 horizontal overflow at 1440/768/390/375 px, TypeScript 0 new errors (4 pre-existing unchanged). `data-testid` coverage: `treatments-page`, `treatments-hero`, `treatments-hero-primary-cta`, `treatments-hero-secondary-cta`, `treatments-intro`, `treatments-grid`, `treatment-card-{ortodontia,alaineri,implanti,estetichna-stomatologia,tmj,sun-i-dishane}`, `treatment-card-{slug}-link`, `treatment-card-soon-{breketi,venci-higiena,detska-stomatologia}`, `treatments-quiz-panel`, `treatments-quiz-cta`, `treatments-care-pass-strip`, `treatments-care-pass-link`, `treatments-faq`, `treatments-faq-{0..5}`, `treatments-final-cta`, `treatments-final-cta-primary`. **Out of scope** (untouched per direction): `/breketi`, `/ortodont/[city]`, analytics events, demo data, admin UI, canonical base centralization, quiz, results, clinic profiles, backend.

- **Pilot Clinic Rename (Feb 2026 — pre-Search-Console cleanup)** — Renamed the active pilot clinic from placeholder `[PILOT — Sofia Premium] (please rename before public traffic)` / slug `pilot-sofia-premium-please-rename-before-public-traffic` to production-safe **Sofia Premium Dental Studio** / slug `sofia-premium-dental-studio`. Direct MongoDB write to `clinics.{id=aa1aa2cd-c794-4f6e-97c3-0e32475925fc}` updating `name`, `clinic_name`, `slug`, `updated_at`; slug-uniqueness pre-check passed. New public profile URL: `https://zubite.bg/kliniki/sofia/invisalign/sofia-premium-dental-studio` (HTTP 200). Old placeholder URL now returns **HTTP 404** via `notFound()` in `app/kliniki/[city]/[specialty]/[clinicSlug]/page.tsx` (no redirect since the placeholder was never indexed). Metadata title, canonical, description, `Dentist` JSON-LD `name`, and BreadcrumbList all rendered with the new name (zero "PILOT"/"please rename" hits anywhere on the page). Sitemap regenerated: still 64 URLs, single clinic profile entry now points to the new slug. Demo/showcase clinics still filtered out (3 hidden). No code changes, no TS impact, no backend test impact.

- **SEO/GEO Phase 2 P0+P1** — Sitemap completeness + internal links + Care Pass metadata hardening + schema enrichment (Feb 2026)** — `app/sitemap.ts` now emits 64 URLs: 8 static (incl. `/care-pass`, `/za-kliniki`, `/standart-za-kliniki`, `/symptoms`), 1 symptom-detail (`/symptoms/bleeding-gums`), 7 SEO content pages, 5 treatment hubs, 15 `/[city]/[treatment]` combos, **new** `/kliniki` root + 3 `/kliniki/[city]` + 18 `/kliniki/[city]/[specialty]` (Bulgarian-friendly slugs: invisalign, aligners, ortodontia, implants, estetichna-stomatologia, detska-stomatologia), **new** dynamic clinic profile URLs `/kliniki/[city]/[specialty]/[slug]` filtered to match the `/api/public/clinics` whitelist (excludes `is_demo`, `is_addons_showcase`, `is_active=false`, and `pending/suspended/draft` statuses), `/blog` index, blog posts. Bug fix: clinic fetch was using `?limit=500` which silently returned `[]` because the endpoint caps at 100 — changed to `?limit=100` and the 1 active pilot clinic now appears (3 demos correctly filtered). Excluded as expected: `/results/**` (noindex via `app/results/layout.tsx`), all `/admin/**`, demo clinics, showcase clinic, paused/suspended clinics. **Internal contextual links to `/kliniki`** added from: homepage ("Виж публичния каталог"), `/orthodontics` (deep-links to `/kliniki/sofia/ortodontia` with copy "разгледаш партньорски клиники с ортодонтски профил"), `/symptoms` ("разгледаш партньорски клиники"), `/care-pass` ("Виж публичния каталог") — all copy avoids "най-добри клиники" / "топ клиники" / "гарантирано подходящи" / paid-ranking implication. **Care Pass metadata hardened**: page `<title>` = "Zubite Care Pass след физическа консултация | Zubite.bg", `description` + OG/Twitter title+description all emphasize "след физическа консултация в участваща партньорска клиника, когато условията са изпълнени" + explicit "Не е застраховка и не е автоматична отстъпка от лечение". FAQ JSON-LD answer also clarifies that online consultation alone does NOT unlock Care Pass. **Schema enrichment**: homepage now emits Organization + WebSite + Service (Service explicitly states "Дентална ориентация и насочване към партньорска клиника" — never implies Zubite is a clinic); `/symptoms` emits MedicalWebPage + BreadcrumbList + ItemList with `lastReviewed`, audience = "Пациенти със зъбни сигнали", description starts with "Информацията е ориентировъчна и не замества преглед при стоматолог"; `/blog` emits Blog + ItemList (4 published posts) + BreadcrumbList; `/za-kliniki` emits Service with `audience: BusinessAudience`, `serviceType: Dental clinic partner program`, and an explicit guardrail in description "Не предлага гарантирано класиране или платена клинична превъзходство". All schemas avoid: claiming Zubite is a clinic, providing diagnoses, "best/top clinic" language, paid-ranking implication. **Tests/smoke**: 5/5 page routes return 200 (`/`, `/kliniki`, `/care-pass`, `/za-kliniki`, `/symptoms`); `/results/[id]` still emits `<meta name="robots" content="noindex, nofollow">`; TypeScript = 0 new errors (4 pre-existing in `lib/api.ts`, `lib/attribution.ts`, `lib/articleTestRender.ts`, `app/admin/dashboard/page.tsx` unchanged by this batch). **Scope-respect**: no new landing pages created — P2 (`/breketi`, `/ortodont/[city]`) remains backlog; no `/treatments` hub; no analytics; no deploy. **Known pre-existing OG fallback copy**: `/orthodontics` and `/symptoms` still inherit `<meta property="og:description" content="Открийте най-доброто решение за вашите зъби.">` from the root layout fallback — not introduced in this batch and not in P0+P1 scope; flagged for SEO Phase 2 P2 follow-up.

- **Phase B — Post-quiz Lead-Capture Gate Live in Real Funnel (June 2026)** — Completed the integration of the `ResultUnlockGate` into the production quiz flow per the MVP unlock-mechanic spec. **`MasterQuiz.tsx` no longer collects name/phone/email/consent** inside the quiz — only city is captured on the final step. `handleSubmit` POSTs an answer-only `LeadCreate` to `/api/leads` which lands as a LOCKED lead (`contact_details_submitted=false`, `full_result_unlocked=false`, `care_pass_eligible=false`, `care_pass_unlocked=false`). On success, the user is redirected to `/results/[leadId]` where `<ResultUnlockGate>` renders. After the gate posts to `POST /api/leads/{id}/unlock-result`, `app/results/[leadId]/page.tsx` redirects to `/quiz/success?leadId=...&stage=...&city=...&name=...&segment=...` so **Manual Recommendation Mode** stays the canonical final state. Backend supports both legacy (full contact upfront) and Phase B (empty contact) shapes via `_validate_quiz_contact`'s "fully populated OR fully empty" rule. `POST /unlock-result` is idempotent — re-unlock never overwrites existing PII, and admin/patient notifications fire only on the False→True transition (first time). `GET /api/leads/{id}` exposes the unlock flags but **never** leaks phone/email/consent and trims `name` to first word only. **Care Pass invariant**: `care_pass_unlocked` stays `False` even after PII submission — it can only be flipped by a clinic-confirmed consultation through Zubite.bg (Phase F, not yet built). Cleanup: removed unused `gaLeadSubmitFiredRef`, `MANUAL_RECOMMENDATION_CTA` import, and `name/phone/email` keys from MasterQuiz `formData` state. **Tests (June 10, 2026)**: 7-step backend curl regression (locked-lead create → GET no-PII → consent-required 422 → happy-path unlock → flag-flip + PII-not-leaked + name-first-word → idempotent re-unlock → original name preserved) ALL PASS. End-to-end UI (desktop 1280×800 + mobile 390×844): 10 quiz answers → soft-commit → city-only form → POST locked lead → redirect to `/results/[id]` → gate visible → fill + consent + submit → redirect to `/quiz/success?leadId=...&stage=high&city=sofia&name=Мария+Тест&segment=adult` → success page shows "Високи риск" band + "Екипът на Zubite.bg преглежда случая ръчно" + Care Pass card. **0px horizontal overflow** on both `/results/[id]` and `/quiz/success` at 390px. TypeScript: 0 new errors. Files changed: `frontend/components/MasterQuiz.tsx`, `frontend/app/results/[leadId]/page.tsx`. Backend unchanged in this batch (already wired in earlier sub-step). Untouched: `routers/public.py` lead-create / unlock-result endpoints, schemas, scoring, attribution, admin, clinic dashboard, blog, SEO landings. **Care Pass copy guardrails verified**: required phrases `отстъпки за продукти за орална хигиена` + `след посетена консултация` present; forbidden phrases (абонамент / застраховка / безплатно лечение) absent. **NO Phase D/E/F/G/H functionality** in this batch — no payments, no booking UI, no slot-locking, no clinic calendar, no online orientation. Manual Recommendation Mode remains the active final state. Documented in CHANGELOG above.


- **New Clinic Pricing Architecture (Feb 2026)** — Full commercial-model overhaul. Public tiers reduced to two: **Verified Profile** (profile-only) and **Growth Partner** (main partner package). **Authority Partner** removed from public. Legacy `partner_tier=premium` clinics migrated to `base_package=growth_partner` + `founding_status=strategic_private` + `legacy_tier=authority_partner` (audit-preserved). New pricing schema on `Clinic`: `base_package`, `founding_status`, `billing_status`, `billing_cadence`, `monthly_price_eur`, `annual_price_eur`, `onboarding_fee_eur`, `founding_start_date`, `founding_end_date`, `private_terms_notes`, `strategic_public_display`, `entitlement_overrides`. Locked defaults auto-fill via `PATCH /api/admin/clinics/{id}`: Verified 39/468/199 · Growth 199/2388/499 · Founding Growth intro €149 × 6 months (order-independent auto-fill — `user_provided_keys` snapshot in `admin_update_clinic`). New `backend/entitlements.py` exposes: `BASE_PACKAGES`, `FOUNDING_STATUS_VALUES`, `BILLING_STATUS_VALUES`, `LEGACY_PARTNER_TIER_TO_BASE_PACKAGE`, `PACKAGE_DEFAULTS`, `default_entitlements`, `apply_addon_entitlements`, `apply_admin_overrides`, `compute_entitlements`, `resolve_base_package`, `resolve_founding_status`, `public_partner_label`, `GUARDRAIL_COPY`. Entitlements matrix (20 flags): Verified gets structured profile + 3 sections + basic performance only. Growth gets enhanced profile + 8 sections + patient journey + analytics dashboard + Care Pass + educational events + monthly mini-report + quarterly optimization + expert Q&A + partner offers + beta access + annual snapshot. Add-ons layer on top: `case_library_asset` flips case-library eligibility; `content_authority` category upgrades spotlight/interview; `analytics_strategy` unlocks dashboard; `partner_campaign` unlocks Care Pass + educational events; `profile_setup:extra_treatment_section` increments section limit. **Add-ons subsystem**: new `backend/addon_catalog.py` seeds 28 items across 6 categories (Content & Authority, Video/Podcast/Event, Analytics & Strategy, Partner Campaign, Profile & Setup, Custom Private). New `backend/routers/clinic_addons.py` — `GET/POST /api/admin/addon-catalog`, `GET/POST/PATCH/DELETE /api/admin/clinics/{id}/addons`, `GET /api/admin/clinics/{id}/entitlements`. **Guardrail hard-coded**: `affects_organic_matching=false` stripped/forced on every write. **Matching**: legacy `premium` boost lowered from +8 to +5 (equal to Growth) — Authority no longer confers hidden ranking advantage. Verified stays in the recommendation flow with 0 boost (soft matching per user choice). `_PLACEMENT_LABEL` no longer says "Premium партньор". **Migration**: server.py startup runs a one-time idempotent backfill (48/48 clinics migrated). **Public payload**: `_public_label_for_clinic()` returns "Verified Profile" or "Growth Partner"; strategic_private only surfaces as "Стратегически партньор" when `strategic_public_display=True`. Legacy premium clinics publicly show "Growth Partner" (never "Authority Partner"). **Admin UI**: `/admin/clinics` gets 6-button package filter row + 6 new columns (Package badge, Founding badge, Billing, Add-ons count, PJ ✓, PA ✓, legacy_tier subtext). `/admin/clinics/[id]` gets new `<ClinicPackageSection>` component with three cards — **Package & Billing** (all fields + 4 conditional warnings + private-terms shield notice + legacy_tier chip), **Current Entitlements** (20 rows with checkmarks + edit-pencil → override modal with mandatory audit note + trash-can), **Add-ons** (existing list with per-row status select + delete, catalog dropdown grouped by 6 categories, custom add-on collapsible form). Guardrail copy footer on every clinic. **Tests**: `/app/backend/tests/test_pricing_architecture.py` — 18/18 pass covering migration, list/detail envelopes, PATCH auto-fill (all 3 packages + founding_growth ordering fix), enum validation, catalog seed, add-on full lifecycle + guardrail, entitlements engine (Verified vs Growth vs case_library_asset flip), public label safety. Testing agent iteration_56: 94% initial (1 real bug fixed), 100% frontend, then 18/18 post-fix.

- **Clinic Archiving + Case Library Revamp (Feb 2026)** — Two admin-facing additions:
  **A) Clinic Archive**: soft-delete flow for partner clinics. New `POST /api/admin/clinics/{id}/archive` sets `archived: true / archived_at`; `POST /api/admin/clinics/{id}/unarchive` reverses. `GET /api/admin/clinics` accepts `?archived=false|true|all` (default excludes archived). Archived clinics are FULLY hidden from every public surface: `_is_clinic_visible` hard-blocks `archived: true`, `_public_clinic_payload` queries filter with `{"archived": {"$ne": True}}`, direct profile URL `/api/public/clinics/{id}` returns 404. Admin UI `/admin/clinics` gets two tabs `Активни` / `Архивирани` (`data-testid="admin-clinics-tab-*"`), an `Архивирай` action on each active row that opens a Bulgarian confirmation modal (`admin-clinic-archive-modal`), and a `Възстанови` action on archived rows. Idempotent for both directions.
  **B) Case Library revamp**: extended `ClinicProfileCase` in `schemas.py` with `treatment_type`, `duration`, `price` (free text — per product decision), `materials`, `specifics`, `before_images[]`, `after_images[]` (max 3 each, URLs ≤500 chars). Admin editor: extracted the previous 950-LOC inline section from `/admin/clinics/[id]/page.tsx` into a new `frontend/components/admin/CaseLibraryEditor.tsx` — collapsible cards per case, datalist-driven Тип лечение (10 curated suggestions + free text), image bank with drag-free 3-thumb grid, `/api/admin/upload` reused for uploads (jpeg/png/webp, ≤5MB), publish blocked until `consent_confirmed`. Public profile `ClinicProfileView.tsx` renders each published + consented case via new `CaseShowcaseCard` (title + category eyebrow, two-column Преди/След image gallery, summary + Особености box, metadata dl with treatment type/duration/price/materials). Payloads in both `public.py` `_safe_clinic_payload` and `public_clinics.py` `_public_clinic_payload` return the new fields — non-published/non-consented cases still stripped. Backend validation: >3 images → 400, non-list → 422, URL >500 chars → 400. **Tests** (`testing_agent_v3_fork` iteration_55): backend 19/19 pytest pass; frontend E2E — archive round-trip, case editor persistence across reload, public rendering all verified at desktop + mobile. Zero TS errors. **Untouched per scope**: quiz, lead capture, Care Pass, matching, JSON-LD, SEO landings, admin/clinic auth. No DB migration.


### P1 — Frontend Backlog
- Centralize data-fetching helper (DRY for fetch patterns across pages).

### P2 — Localization
- English version under `/en/...`.

### Content Automation Roadmap
- ✅ Phase 1 — Backend foundation (parser + Make.com webhooks + jobs collection) — Feb 2026
- ✅ Phase 2 — Admin Content Automation page (`/admin/content-automation`) — Feb 2026
- ✅ Phase 3 — Image Requirements panel + manual + bulk + matching — Feb 2026
- ✅ Phase 4 — Public render-time placeholder replacement + publish protection — Feb 2026

### Backlog (Content Automation P3+)
- Top statistics cards above jobs table (count this month, avg time-to-draft, % needing images)
- Image-completion mini-badge on "Open Draft" link (e.g. `2/3 images`)
- ZIP importer fallback (`POST /api/admin/content-automation/import-from-zip`)

### P3 — Admin CMS
- Inline editing for homepage text, treatment details.

### P4 — Geo Expansion
- Add Varna, Burgas, Ruse to city dropdown + service availability.

### P5 — Infrastructure
- Replace in-memory rate limiter with Redis when horizontally scaling.
- Add JWT_SECRET fail-fast unit test (subprocess boot).

## Test Credentials
See `/app/memory/test_credentials.md`.
